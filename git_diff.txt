diff --git a/desktopApp/src/desktopMain/kotlin/com/app/biashara/ui/screens/DesktopScreens.kt b/desktopApp/src/desktopMain/kotlin/com/app/biashara/ui/screens/DesktopScreens.kt
index 3901825..fb9e9c3 100644
--- a/desktopApp/src/desktopMain/kotlin/com/app/biashara/ui/screens/DesktopScreens.kt
+++ b/desktopApp/src/desktopMain/kotlin/com/app/biashara/ui/screens/DesktopScreens.kt
@@ -16,6 +16,9 @@ import androidx.compose.ui.unit.sp
 import androidx.compose.material.icons.Icons
 import androidx.compose.material.icons.filled.*
 import androidx.compose.material3.*
+import androidx.compose.ui.graphics.Path
+import androidx.compose.ui.graphics.PathEffect
+import androidx.compose.ui.graphics.drawscope.Stroke
 import java.io.File
 import androidx.compose.runtime.*
 import androidx.compose.ui.Alignment
@@ -24,6 +27,14 @@ import com.app.biashara.ui.theme.*
 import com.app.biashara.UserSession
 import com.app.biashara.presentation.viewmodel.*
 import com.app.biashara.domain.model.*
+import androidx.compose.ui.text.input.PasswordVisualTransformation
+import com.app.biashara.domain.usecase.generateId
+import androidx.compose.foundation.text.KeyboardOptions
+import androidx.compose.ui.text.input.KeyboardType
+import kotlinx.datetime.Clock
+import kotlinx.datetime.TimeZone
+import kotlinx.datetime.toLocalDateTime
+import kotlinx.datetime.Instant
 
 inline fun <reified T : Any> inject(): T = org.koin.core.context.GlobalContext.get().get()
 
@@ -31,11 +42,16 @@ inline fun <reified T : Any> inject(): T = org.koin.core.context.GlobalContext.g
 
 @Composable
 fun DesktopDashboardScreen(
-    viewModel: DashboardViewModel = remember { inject() }
+    viewModel: DashboardViewModel = remember { inject() },
+    socialViewModel: SocialViewModel = remember { inject() },
+    navigationViewModel: com.app.biashara.ui.DesktopNavigationViewModel = remember { inject() }
 ) {
     val state by viewModel.state.collectAsState()
+    val socialState by socialViewModel.state.collectAsState()
+
     LaunchedEffect(Unit) {
         viewModel.loadDashboard()
+        socialViewModel.loadChannelsAndInbox()
     }
 
     val scrollState = rememberScrollState()
@@ -48,11 +64,91 @@ fun DesktopDashboardScreen(
         verticalArrangement = Arrangement.spacedBy(24.dp)
     ) {
         // Dashboard title
-        Text(
-            text = "Dashboard",
-            style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
-            color = Color(0xFF1E293B)
-        )
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Dashboard",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Overview of your business performance",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        // Social Onboarding Banner
+        if (socialState.channels.isEmpty() && !socialState.isLoading) {
+            Card(
+                modifier = Modifier.fillMaxWidth(),
+                shape = RoundedCornerShape(12.dp),
+                colors = CardDefaults.cardColors(containerColor = Color.White),
+                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+            ) {
+                Row(
+                    modifier = Modifier
+                        .fillMaxWidth()
+                        .background(
+                            Brush.horizontalGradient(
+                                colors = listOf(
+                                    Color(0xFFE6F7F0),
+                                    Color(0xFFE0F2FE)
+                                )
+                            )
+                        )
+                        .padding(20.dp),
+                    horizontalArrangement = Arrangement.SpaceBetween,
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    Row(
+                        modifier = Modifier.weight(1f),
+                        verticalAlignment = Alignment.CenterVertically,
+                        horizontalArrangement = Arrangement.spacedBy(16.dp)
+                    ) {
+                        Box(
+                            modifier = Modifier
+                                .size(48.dp)
+                                .clip(CircleShape)
+                                .background(B360Green.copy(0.1f)),
+                            contentAlignment = Alignment.Center
+                        ) {
+                            Icon(
+                                imageVector = Icons.Default.Forum,
+                                contentDescription = "Social Commerce",
+                                tint = B360Green,
+                                modifier = Modifier.size(24.dp)
+                            )
+                        }
+                        Column {
+                            Text(
+                                text = "Unleash Social Commerce AI Sales",
+                                style = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold),
+                                color = Color(0xFF1E293B)
+                            )
+                            Spacer(Modifier.height(4.dp))
+                            Text(
+                                text = "Link WhatsApp, Instagram, Facebook, or TikTok. Respond instantly with catalog items and trigger secure M-Pesa checkouts directly in customer chats.",
+                                style = MaterialTheme.typography.bodyMedium,
+                                color = Color(0xFF475569)
+                            )
+                        }
+                    }
+                    Button(
+                        onClick = { navigationViewModel.navigateTo(com.app.biashara.ui.AppScreen.Social) },
+                        colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                        shape = RoundedCornerShape(8.dp)
+                    ) {
+                        Row(
+                            verticalAlignment = Alignment.CenterVertically,
+                            horizontalArrangement = Arrangement.spacedBy(8.dp)
+                        ) {
+                            Text("Connect Channels", fontWeight = FontWeight.Bold)
+                            Icon(Icons.Default.ArrowForward, contentDescription = null, modifier = Modifier.size(16.dp))
+                        }
+                    }
+                }
+            }
+        }
 
         // KPI cards row
         Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
@@ -454,6 +550,66 @@ fun TopCustomerRow(name: String, orders: String, spent: String) {
 
 // ─── Inventory ────────────────────────────────────────────────────────────────
 
+@Composable
+fun InventoryEmptyStateIllustration(modifier: Modifier = Modifier) {
+    Canvas(modifier = modifier.size(160.dp)) {
+        val w = size.width
+        val h = size.height
+        
+        // Isometric crate draw logic
+        val leftFace = Path().apply {
+            moveTo(w * 0.5f, h * 0.75f)
+            lineTo(w * 0.25f, h * 0.6f)
+            lineTo(w * 0.25f, h * 0.35f)
+            lineTo(w * 0.5f, h * 0.5f)
+            close()
+        }
+        val rightFace = Path().apply {
+            moveTo(w * 0.5f, h * 0.75f)
+            lineTo(w * 0.75f, h * 0.6f)
+            lineTo(w * 0.75f, h * 0.35f)
+            lineTo(w * 0.5f, h * 0.5f)
+            close()
+        }
+        val topFace = Path().apply {
+            moveTo(w * 0.5f, h * 0.5f)
+            lineTo(w * 0.25f, h * 0.35f)
+            lineTo(w * 0.5f, h * 0.2f)
+            lineTo(w * 0.75f, h * 0.35f)
+            close()
+        }
+        
+        drawPath(leftFace, Color(0xFFD7CCC8))
+        drawPath(rightFace, Color(0xFFBCAAA4))
+        drawPath(topFace, Color(0xFFEFEBE9))
+        
+        val plankXLeft1 = Path().apply {
+            moveTo(w * 0.25f, h * 0.6f)
+            lineTo(w * 0.5f, h * 0.5f)
+        }
+        val plankXLeft2 = Path().apply {
+            moveTo(w * 0.25f, h * 0.35f)
+            lineTo(w * 0.5f, h * 0.75f)
+        }
+        drawPath(plankXLeft1, Color(0xFF8D6E63), style = Stroke(width = 4f))
+        drawPath(plankXLeft2, Color(0xFF8D6E63), style = Stroke(width = 4f))
+
+        val plankXRight1 = Path().apply {
+            moveTo(w * 0.75f, h * 0.6f)
+            lineTo(w * 0.5f, h * 0.5f)
+        }
+        val plankXRight2 = Path().apply {
+            moveTo(w * 0.75f, h * 0.35f)
+            lineTo(w * 0.5f, h * 0.75f)
+        }
+        drawPath(plankXRight1, Color(0xFF8D6E63), style = Stroke(width = 4f))
+        drawPath(plankXRight2, Color(0xFF8D6E63), style = Stroke(width = 4f))
+        
+        drawCircle(Color(0xFF10B981), radius = 6f, center = androidx.compose.ui.geometry.Offset(w * 0.2f, h * 0.25f))
+        drawCircle(Color(0xFF10B981).copy(alpha = 0.5f), radius = 4f, center = androidx.compose.ui.geometry.Offset(w * 0.85f, h * 0.4f))
+    }
+}
+
 @Composable
 fun DesktopInventoryScreen(
     viewModel: InventoryViewModel = remember { inject() }
@@ -463,592 +619,3381 @@ fun DesktopInventoryScreen(
         viewModel.loadProducts(UserSession.getBusinessId())
     }
 
+    var showAddProductDialog by remember { mutableStateOf(false) }
+    var editingProduct by remember { mutableStateOf<Product?>(null) }
+
     var searchQuery by remember { mutableStateOf("") }
     LaunchedEffect(searchQuery) {
         viewModel.onSearchQueryChange(searchQuery)
     }
 
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
-        Row(horizontalArrangement = Arrangement.spacedBy(12.dp), verticalAlignment = Alignment.CenterVertically) {
-            OutlinedTextField(
-                value = searchQuery, onValueChange = { searchQuery = it },
-                placeholder = { Text("Search products...") },
-                leadingIcon = { Icon(Icons.Filled.Search, null) },
-                modifier = Modifier.width(320.dp), shape = RoundedCornerShape(10.dp), singleLine = true
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Inventory",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Manage your products and stock levels",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
             )
-            Spacer(Modifier.weight(1f))
-            Button(onClick = {}, colors = ButtonDefaults.buttonColors(B360Green)) {
-                Icon(Icons.Filled.Add, null, Modifier.size(16.dp))
-                Spacer(Modifier.width(6.dp))
-                Text("Add Product")
-            }
         }
 
-        Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(12.dp)) {
-            Column {
-                // Header
-                Row(Modifier.fillMaxWidth().background(Color(0xFFF8F8F8)).padding(horizontal = 16.dp, vertical = 12.dp)) {
-                    listOf("Product Name", "SKU", "Buying Price", "Selling Price", "Stock", "Status", "Actions").forEachIndexed { i, header ->
-                        Text(header, fontWeight = FontWeight.Bold, fontSize = 12.sp, color = Color.Gray,
-                            modifier = Modifier.weight(if (i == 0) 2f else 1f))
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+                Row(
+                    horizontalArrangement = Arrangement.spacedBy(12.dp),
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    OutlinedTextField(
+                        value = searchQuery,
+                        onValueChange = { searchQuery = it },
+                        placeholder = { Text("Search products by name or SKU...", color = Color(0xFF94A3B8)) },
+                        leadingIcon = { Icon(Icons.Filled.Search, null, tint = Color(0xFF94A3B8), modifier = Modifier.size(18.dp)) },
+                        modifier = Modifier.weight(1f),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true,
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
+                    )
+                    OutlinedButton(
+                        onClick = {},
+                        shape = RoundedCornerShape(8.dp),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+                    ) {
+                        Icon(Icons.Filled.FilterList, null, Modifier.size(16.dp))
+                        Spacer(Modifier.width(6.dp))
+                        Text("Filter")
+                    }
+                    Button(
+                        onClick = { showAddProductDialog = true },
+                        shape = RoundedCornerShape(8.dp),
+                        colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                    ) {
+                        Icon(Icons.Filled.Add, null, Modifier.size(16.dp), tint = Color.White)
+                        Spacer(Modifier.width(6.dp))
+                        Text("Add Product", color = Color.White)
                     }
                 }
-                HorizontalDivider()
+
                 if (state.filteredProducts.isEmpty()) {
-                    Text("No products found", color = Color.Gray, modifier = Modifier.padding(24.dp).align(Alignment.CenterHorizontally))
+                    Column(
+                        modifier = Modifier.fillMaxWidth().padding(vertical = 48.dp),
+                        horizontalAlignment = Alignment.CenterHorizontally,
+                        verticalArrangement = Arrangement.spacedBy(16.dp)
+                    ) {
+                        InventoryEmptyStateIllustration()
+                        Text("No products found", style = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold), color = Color(0xFF1E293B))
+                        Text("Get started by adding your first product to your store.", style = MaterialTheme.typography.bodyMedium, color = Color(0xFF64748B))
+                        Button(
+                            onClick = { showAddProductDialog = true },
+                            shape = RoundedCornerShape(8.dp),
+                            colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                        ) {
+                            Icon(Icons.Filled.Add, null, Modifier.size(16.dp), tint = Color.White)
+                            Spacer(Modifier.width(6.dp))
+                            Text("Add Product", color = Color.White)
+                        }
+                    }
                 } else {
-                    state.filteredProducts.forEach { product ->
-                        val statusColor = if (product.isOutOfStock) B360Red else if (product.isLowStock) B360Amber else B360Green
-                        val statusText = if (product.isOutOfStock) "OUT" else if (product.isLowStock) "LOW" else "OK"
-                        Row(Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 12.dp), verticalAlignment = Alignment.CenterVertically) {
-                            Text(product.name, modifier = Modifier.weight(2f), fontWeight = FontWeight.Medium)
-                            Text(product.sku, modifier = Modifier.weight(1f), color = Color.Gray, fontSize = 13.sp)
-                            Text("KES ${String.format("%,.0f", product.buyingPrice)}", modifier = Modifier.weight(1f))
-                            Text("KES ${String.format("%,.0f", product.sellingPrice)}", modifier = Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold)
-                            Text(product.currentStock.toString(), modifier = Modifier.weight(1f), fontWeight = FontWeight.Bold, color = statusColor)
-                            Surface(color = statusColor.copy(0.1f), shape = RoundedCornerShape(20.dp), modifier = Modifier.weight(1f)) {
-                                Text(statusText, color = statusColor, fontSize = 11.sp, modifier = Modifier.padding(horizontal = 8.dp, vertical = 3.dp))
-                            }
-                            Row(Modifier.weight(1f), horizontalArrangement = Arrangement.spacedBy(4.dp)) {
-                                IconButton(onClick = {}, modifier = Modifier.size(28.dp)) { Icon(Icons.Filled.Edit, null, tint = B360Blue, modifier = Modifier.size(16.dp)) }
-                                IconButton(onClick = {}, modifier = Modifier.size(28.dp)) { Icon(Icons.Filled.AddBox, null, tint = B360Green, modifier = Modifier.size(16.dp)) }
+                    Column {
+                        Row(
+                            Modifier
+                                .fillMaxWidth()
+                                .background(Color(0xFFF8FAFC))
+                                .padding(horizontal = 16.dp, vertical = 12.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            val headers = listOf("Product Name", "SKU", "Buying Price", "Selling Price", "Stock", "Status", "Actions")
+                            headers.forEachIndexed { i, header ->
+                                Row(
+                                    modifier = Modifier.weight(if (i == 0) 2f else 1f),
+                                    verticalAlignment = Alignment.CenterVertically,
+                                    horizontalArrangement = Arrangement.spacedBy(4.dp)
+                                ) {
+                                    Text(
+                                        text = header,
+                                        fontWeight = FontWeight.SemiBold,
+                                        fontSize = 13.sp,
+                                        color = Color(0xFF64748B)
+                                    )
+                                    if (header != "Actions") {
+                                        Icon(
+                                            imageVector = Icons.Filled.SwapVert,
+                                            contentDescription = "Sort",
+                                            tint = Color(0xFF94A3B8),
+                                            modifier = Modifier.size(14.dp)
+                                        )
+                                    }
+                                }
                             }
                         }
-                        HorizontalDivider(color = Color(0xFFF5F5F5))
-                    }
-                }
-            }
-        }
-    }
-}
-
-// ─── Orders ───────────────────────────────────────────────────────────────────
-
-@Composable
-fun DesktopOrdersScreen(
-    viewModel: OrdersViewModel = remember { inject() }
-) {
-    val state by viewModel.state.collectAsState()
-    LaunchedEffect(Unit) {
-        viewModel.loadOrders()
-    }
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
 
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
-        Row(verticalAlignment = Alignment.CenterVertically) {
-            Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {
-                FilterChip(selected = state.selectedTabStatus == null, onClick = { viewModel.selectTab(null) }, label = { Text("All") })
-                FilterChip(selected = state.selectedTabStatus == PaymentStatus.PAID, onClick = { viewModel.selectTab(PaymentStatus.PAID) }, label = { Text("Paid") })
-                FilterChip(selected = state.selectedTabStatus == PaymentStatus.PENDING, onClick = { viewModel.selectTab(PaymentStatus.PENDING) }, label = { Text("Pending") })
-                FilterChip(selected = state.selectedTabStatus == PaymentStatus.COD, onClick = { viewModel.selectTab(PaymentStatus.COD) }, label = { Text("COD") })
-            }
-            Spacer(Modifier.weight(1f))
-            Button(onClick = {}, colors = ButtonDefaults.buttonColors(B360Green)) {
-                Icon(Icons.Filled.Add, null, Modifier.size(16.dp))
-                Spacer(Modifier.width(6.dp))
-                Text("New Order")
-            }
-        }
-
-        Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(12.dp)) {
-            Column {
-                Row(Modifier.fillMaxWidth().background(Color(0xFFF8F8F8)).padding(horizontal = 16.dp, vertical = 12.dp)) {
-                    listOf("Order #", "Customer", "Phone", "Amount", "Payment", "Delivery", "Date").forEachIndexed { i, h ->
-                        Text(h, fontWeight = FontWeight.Bold, fontSize = 12.sp, color = Color.Gray, modifier = Modifier.weight(if (i == 0) 1f else 1.2f))
-                    }
-                }
-                HorizontalDivider()
-                if (state.filteredOrders.isEmpty()) {
-                    Text("No orders found", color = Color.Gray, modifier = Modifier.padding(24.dp).align(Alignment.CenterHorizontally))
-                } else {
-                    state.filteredOrders.forEach { order ->
-                        val payColor = when (order.paymentStatus) { PaymentStatus.PAID -> B360Green; PaymentStatus.PENDING -> B360Amber; else -> B360Blue }
-                        Row(Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 13.dp), verticalAlignment = Alignment.CenterVertically) {
-                            Text(order.orderNumber, modifier = Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold, fontSize = 13.sp)
-                            Text(order.customerName, modifier = Modifier.weight(1.2f), fontWeight = FontWeight.Medium)
-                            Text(order.customerPhone, modifier = Modifier.weight(1.2f), color = Color.Gray, fontSize = 13.sp)
-                            Text("KES ${String.format("%,.0f", order.subtotal)}", modifier = Modifier.weight(1.2f), fontWeight = FontWeight.Bold)
-                            Surface(color = payColor.copy(0.1f), shape = RoundedCornerShape(20.dp), modifier = Modifier.weight(1.2f)) {
-                                Text(order.paymentStatus.name, color = payColor, fontSize = 11.sp, modifier = Modifier.padding(horizontal = 8.dp, vertical = 3.dp))
+                        state.filteredProducts.forEach { product ->
+                            val statusColor = if (product.isOutOfStock) B360Red else if (product.isLowStock) B360Amber else B360Green
+                            val statusText = if (product.isOutOfStock) "Out of Stock" else if (product.isLowStock) "Low Stock" else "In Stock"
+                            
+                            Row(
+                                Modifier
+                                    .fillMaxWidth()
+                                    .padding(horizontal = 16.dp, vertical = 14.dp),
+                                verticalAlignment = Alignment.CenterVertically
+                            ) {
+                                Text(product.name, modifier = Modifier.weight(2f), fontWeight = FontWeight.Medium, color = Color(0xFF1E293B))
+                                Text(product.sku, modifier = Modifier.weight(1f), color = Color(0xFF64748B), fontSize = 13.sp)
+                                Text("KES ${String.format("%,.0f", product.buyingPrice)}", modifier = Modifier.weight(1f), color = Color(0xFF1E293B))
+                                Text("KES ${String.format("%,.0f", product.sellingPrice)}", modifier = Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold)
+                                Text(product.currentStock.toString(), modifier = Modifier.weight(1f), fontWeight = FontWeight.Bold, color = statusColor)
+                                
+                                Box(modifier = Modifier.weight(1f)) {
+                                    Surface(
+                                        color = statusColor.copy(0.1f),
+                                        shape = RoundedCornerShape(20.dp)
+                                    ) {
+                                        Text(
+                                            text = statusText,
+                                            color = statusColor,
+                                            fontSize = 11.sp,
+                                            fontWeight = FontWeight.SemiBold,
+                                            modifier = Modifier.padding(horizontal = 10.dp, vertical = 4.dp)
+                                        )
+                                    }
+                                }
+                                
+                                Row(
+                                    Modifier.weight(1f),
+                                    horizontalArrangement = Arrangement.spacedBy(8.dp),
+                                    verticalAlignment = Alignment.CenterVertically
+                                ) {
+                                    IconButton(
+                                        onClick = { editingProduct = product },
+                                        modifier = Modifier
+                                            .size(32.dp)
+                                            .border(1.dp, Color(0xFFE2E8F0), RoundedCornerShape(6.dp))
+                                    ) {
+                                        Icon(
+                                            imageVector = Icons.Filled.Edit,
+                                            contentDescription = "Edit",
+                                            tint = Color(0xFF64748B),
+                                            modifier = Modifier.size(16.dp)
+                                        )
+                                    }
+                                    IconButton(
+                                        onClick = {},
+                                        modifier = Modifier
+                                            .size(32.dp)
+                                            .border(1.dp, Color(0xFFE2E8F0), RoundedCornerShape(6.dp))
+                                    ) {
+                                        Icon(
+                                            imageVector = Icons.Filled.AddBox,
+                                            contentDescription = "Restock",
+                                            tint = B360Green,
+                                            modifier = Modifier.size(16.dp)
+                                        )
+                                    }
+                                }
                             }
-                            Text(order.deliveryStatus.name, modifier = Modifier.weight(1.2f), fontSize = 12.sp, color = Color.Gray)
-                            Text(order.createdAt.toString().take(16).replace("T", " "), modifier = Modifier.weight(1.2f), fontSize = 12.sp, color = Color.Gray)
+                            HorizontalDivider(color = Color(0xFFF1F5F9))
                         }
-                        HorizontalDivider(color = Color(0xFFF5F5F5))
                     }
-                }
-            }
-        }
-    }
-}
-
-// ─── Customers ────────────────────────────────────────────────────────────────
-
-@Composable
-fun DesktopCustomersScreen(
-    viewModel: CustomersViewModel = remember { inject() }
-) {
-    val state by viewModel.state.collectAsState()
-    LaunchedEffect(Unit) {
-        viewModel.loadCustomers()
-    }
-    var searchQuery by remember { mutableStateOf("") }
-    LaunchedEffect(searchQuery) {
-        viewModel.onSearchQueryChange(searchQuery)
-    }
 
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
-        Row(verticalAlignment = Alignment.CenterVertically) {
-            OutlinedTextField(value = searchQuery, onValueChange = { searchQuery = it }, placeholder = { Text("Search customers...") },
-                leadingIcon = { Icon(Icons.Filled.Search, null) }, modifier = Modifier.width(320.dp), shape = RoundedCornerShape(10.dp), singleLine = true)
-            Spacer(Modifier.weight(1f))
-            Button(onClick = {}, colors = ButtonDefaults.buttonColors(B360Green)) {
-                Icon(Icons.Filled.PersonAdd, null, Modifier.size(16.dp)); Spacer(Modifier.width(6.dp)); Text("Add Customer")
-            }
-        }
-
-        Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(12.dp)) {
-            Column {
-                Row(Modifier.fillMaxWidth().background(Color(0xFFF8F8F8)).padding(horizontal = 16.dp, vertical = 12.dp)) {
-                    listOf("Customer", "Phone", "Orders", "Total Spent", "Loyalty Pts", "Actions").forEachIndexed { i, h ->
-                        Text(h, fontWeight = FontWeight.Bold, fontSize = 12.sp, color = Color.Gray, modifier = Modifier.weight(if (i == 0) 1.5f else 1f))
-                    }
-                }
-                HorizontalDivider()
-                if (state.filteredCustomers.isEmpty()) {
-                    Text("No customers found", color = Color.Gray, modifier = Modifier.padding(24.dp).align(Alignment.CenterHorizontally))
-                } else {
-                    state.filteredCustomers.forEach { customer ->
-                        Row(Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 12.dp), verticalAlignment = Alignment.CenterVertically) {
-                            Row(Modifier.weight(1.5f), horizontalArrangement = Arrangement.spacedBy(10.dp), verticalAlignment = Alignment.CenterVertically) {
-                                Box(Modifier.size(32.dp).background(B360Green.copy(0.1f), RoundedCornerShape(50)), contentAlignment = Alignment.Center) {
-                                    Text(customer.name.firstOrNull()?.toString()?.uppercase() ?: "", color = B360Green, fontWeight = FontWeight.Bold)
-                                }
-                                Text(customer.name, fontWeight = FontWeight.Medium)
+                    Row(
+                        modifier = Modifier.fillMaxWidth().padding(top = 12.dp),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(
+                            text = "Showing 1 to ${state.filteredProducts.size} of ${state.filteredProducts.size} products",
+                            fontSize = 13.sp,
+                            color = Color(0xFF64748B)
+                        )
+                        Row(
+                            horizontalArrangement = Arrangement.spacedBy(6.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowLeft, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
                             }
-                            Text(customer.phone, Modifier.weight(1f), color = Color.Gray, fontSize = 13.sp)
-                            Text("—", Modifier.weight(1f), fontWeight = FontWeight.SemiBold)
-                            Text("—", Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold)
-                            Row(Modifier.weight(1f), horizontalArrangement = Arrangement.spacedBy(4.dp), verticalAlignment = Alignment.CenterVertically) {
-                                Icon(Icons.Filled.Star, null, tint = B360Amber, modifier = Modifier.size(14.dp))
-                                Text(customer.loyaltyPoints.toString(), fontWeight = FontWeight.Medium)
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, B360Green),
+                                colors = ButtonDefaults.outlinedButtonColors(containerColor = B360Green.copy(0.08f), contentColor = B360Green)
+                            ) {
+                                Text("1", fontWeight = FontWeight.Bold, fontSize = 13.sp)
                             }
-                            Row(Modifier.weight(1f), horizontalArrangement = Arrangement.spacedBy(4.dp)) {
-                                IconButton(onClick = {}, modifier = Modifier.size(28.dp)) { Icon(Icons.Filled.Visibility, null, tint = B360Blue, modifier = Modifier.size(16.dp)) }
-                                IconButton(onClick = {}, modifier = Modifier.size(28.dp)) { Icon(Icons.Filled.Chat, null, tint = B360Green, modifier = Modifier.size(16.dp)) }
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowRight, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
                             }
                         }
-                        HorizontalDivider(color = Color(0xFFF5F5F5))
-                    }
-                }
             }
         }
     }
-}
 
-// ─── Expenses ─────────────────────────────────────────────────────────────────
-
-@Composable
-fun DesktopExpensesScreen(
-    viewModel: ExpensesViewModel = remember { inject() }
-) {
-    val state by viewModel.state.collectAsState()
-    LaunchedEffect(Unit) {
-        viewModel.loadExpenses()
-    }
+    if (showAddProductDialog || editingProduct != null) {
+        val isEdit = editingProduct != null
+        var name by remember { mutableStateOf(editingProduct?.name ?: "") }
+        var sku by remember { mutableStateOf(editingProduct?.sku ?: "") }
+        var buyingPrice by remember { mutableStateOf(editingProduct?.buyingPrice?.toString() ?: "") }
+        var sellingPrice by remember { mutableStateOf(editingProduct?.sellingPrice?.toString() ?: "") }
+        var currentStock by remember { mutableStateOf(editingProduct?.currentStock?.toString() ?: "") }
+        var error by remember { mutableStateOf<String?>(null) }
 
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
-        Row(horizontalArrangement = Arrangement.spacedBy(12.dp)) {
-            SummaryStatCard(Modifier.weight(1f), "Total This Month", "KES ${String.format("%,.0f", state.totalAmount)}", B360Red)
-            SummaryStatCard(Modifier.weight(1f), "Advertising", "KES 8,500", B360Blue)
-            SummaryStatCard(Modifier.weight(1f), "Stock Purchase", "KES 45,000", B360Green)
-            SummaryStatCard(Modifier.weight(1f), "Operations", "KES 12,000", B360Amber)
-        }
-        Row(verticalAlignment = Alignment.CenterVertically) {
-            Spacer(Modifier.weight(1f))
-            Button(onClick = {}, colors = ButtonDefaults.buttonColors(B360Green)) {
-                Icon(Icons.Filled.Add, null, Modifier.size(16.dp)); Spacer(Modifier.width(6.dp)); Text("Add Expense")
-            }
-        }
-        Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(12.dp)) {
-            Column {
-                Row(Modifier.fillMaxWidth().background(Color(0xFFF8F8F8)).padding(16.dp, 12.dp)) {
-                    listOf("Description", "Category", "Amount", "Date", "Actions").forEachIndexed { i, h ->
-                        Text(h, fontWeight = FontWeight.Bold, fontSize = 12.sp, color = Color.Gray, modifier = Modifier.weight(if (i == 0) 2f else 1f))
+        AlertDialog(
+            onDismissRequest = {
+                showAddProductDialog = false
+                editingProduct = null
+            },
+            title = {
+                Text(
+                    text = if (isEdit) "Edit Product" else "Add New Product",
+                    fontWeight = FontWeight.Bold,
+                    color = Color(0xFF1E293B)
+                )
+            },
+            text = {
+                Column(
+                    verticalArrangement = Arrangement.spacedBy(12.dp),
+                    modifier = Modifier.width(360.dp)
+                ) {
+                    if (error != null) {
+                        Text(error!!, color = B360Red, fontSize = 12.sp, fontWeight = FontWeight.Bold)
+                    }
+                    OutlinedTextField(
+                        value = name,
+                        onValueChange = { name = it },
+                        label = { Text("Product Name *") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
+                    OutlinedTextField(
+                        value = sku,
+                        onValueChange = { sku = it },
+                        label = { Text("SKU / Barcode *") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
+                    Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {
+                        OutlinedTextField(
+                            value = buyingPrice,
+                            onValueChange = { buyingPrice = it },
+                            label = { Text("Cost Price *") },
+                            modifier = Modifier.weight(1f),
+                            shape = RoundedCornerShape(8.dp),
+                            singleLine = true
+                        )
+                        OutlinedTextField(
+                            value = sellingPrice,
+                            onValueChange = { sellingPrice = it },
+                            label = { Text("Sell Price *") },
+                            modifier = Modifier.weight(1f),
+                            shape = RoundedCornerShape(8.dp),
+                            singleLine = true
+                        )
                     }
+                    OutlinedTextField(
+                        value = currentStock,
+                        onValueChange = { currentStock = it },
+                        label = { Text("Stock Quantity *") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
                 }
-                HorizontalDivider()
-                if (state.expenses.isEmpty()) {
-                    Text("No expenses recorded", color = Color.Gray, modifier = Modifier.padding(24.dp).align(Alignment.CenterHorizontally))
-                } else {
-                    state.expenses.forEach { expense ->
-                        val catColor = when (expense.category) {
-                            ExpenseCategory.ADVERTISING -> B360Blue
-                            ExpenseCategory.RENT -> B360Red
-                            ExpenseCategory.STOCK_PURCHASE -> B360Green
-                            ExpenseCategory.DELIVERY -> B360Amber
-                            else -> Color.Gray
+            },
+            confirmButton = {
+                Button(
+                    onClick = {
+                        val cost = buyingPrice.toDoubleOrNull()
+                        val sell = sellingPrice.toDoubleOrNull()
+                        val stock = currentStock.toIntOrNull()
+
+                        if (cost == null || sell == null || stock == null) {
+                            error = "Please enter valid numeric values for prices and stock."
+                            return@Button
                         }
-                        Row(Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 12.dp), verticalAlignment = Alignment.CenterVertically) {
-                            Text(expense.description, Modifier.weight(2f), fontWeight = FontWeight.Medium)
-                            Surface(color = catColor.copy(0.1f), shape = RoundedCornerShape(20.dp), modifier = Modifier.weight(1f)) {
-                                Text(expense.category.displayName(), color = catColor, fontSize = 11.sp, modifier = Modifier.padding(horizontal = 8.dp, vertical = 3.dp))
-                            }
-                            Text("KES ${String.format("%,.0f", expense.amount)}", Modifier.weight(1f), color = B360Red, fontWeight = FontWeight.SemiBold)
-                            Text(expense.expenseDate.toString().take(10), Modifier.weight(1f), color = Color.Gray, fontSize = 13.sp)
-                            IconButton(onClick = { viewModel.deleteExpense(expense.id) }, Modifier.weight(1f).size(28.dp)) { Icon(Icons.Filled.Delete, null, tint = B360Red, modifier = Modifier.size(16.dp)) }
+
+                        if (name.isBlank() || sku.isBlank()) {
+                            error = "Product Name and SKU are required."
+                            return@Button
                         }
-                        HorizontalDivider(color = Color(0xFFF5F5F5))
+
+                        val product = Product(
+                            id = editingProduct?.id ?: generateId(),
+                            businessId = UserSession.getBusinessId(),
+                            name = name,
+                            sku = sku,
+                            buyingPrice = cost,
+                            sellingPrice = sell,
+                            currentStock = stock,
+                            createdAt = editingProduct?.createdAt ?: Clock.System.now(),
+                            updatedAt = Clock.System.now()
+                        )
+                        viewModel.saveProduct(product)
+                        showAddProductDialog = false
+                        editingProduct = null
+                    },
+                    colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                ) {
+                    Text("Save", color = Color.White)
+                }
+            },
+            dismissButton = {
+                TextButton(
+                    onClick = {
+                        showAddProductDialog = false
+                        editingProduct = null
                     }
+                ) {
+                    Text("Cancel", color = Color.Gray)
                 }
             }
-        }
+        )
     }
 }
 
+// ─── Orders ───────────────────────────────────────────────────────────────────
+
 @Composable
-fun SummaryStatCard(modifier: Modifier, label: String, value: String, color: Color) {
-    Card(modifier, shape = RoundedCornerShape(12.dp), colors = CardDefaults.cardColors(color.copy(0.08f))) {
-        Column(Modifier.padding(16.dp)) {
-            Text(label, fontSize = 12.sp, color = color.copy(0.8f))
-            Text(value, fontWeight = FontWeight.Bold, fontSize = 18.sp, color = color)
+fun OrdersEmptyStateIllustration(modifier: Modifier = Modifier) {
+    Canvas(modifier = modifier.size(160.dp)) {
+        val w = size.width
+        val h = size.height
+        
+        // Dotted trajectory line representing paper airplane flight path
+        val trajPath = Path().apply {
+            moveTo(w * 0.45f, h * 0.65f)
+            quadraticBezierTo(w * 0.5f, h * 0.45f, w * 0.65f, h * 0.35f)
+        }
+        drawPath(
+            path = trajPath,
+            color = Color(0xFF10B981).copy(0.4f),
+            style = Stroke(
+                width = 2.dp.toPx(),
+                pathEffect = PathEffect.dashPathEffect(floatArrayOf(10f, 10f), 0f)
+            )
+        )
+        
+        // Box front-left face
+        val pathFrontLeft = Path().apply {
+            moveTo(w * 0.3f, h * 0.62f)
+            lineTo(w * 0.5f, h * 0.72f)
+            lineTo(w * 0.5f, h * 0.9f)
+            lineTo(w * 0.3f, h * 0.8f)
+            close()
+        }
+        drawPath(pathFrontLeft, Color(0xFFA7F3D0)) // Soft mint green
+        
+        // Box front-right face
+        val pathFrontRight = Path().apply {
+            moveTo(w * 0.5f, h * 0.72f)
+            lineTo(w * 0.7f, h * 0.62f)
+            lineTo(w * 0.7f, h * 0.8f)
+            lineTo(w * 0.5f, h * 0.9f)
+            close()
+        }
+        drawPath(pathFrontRight, Color(0xFF6EE7B7)) // Slightly darker mint green
+        
+        // Box inside-left face
+        val pathInsideLeft = Path().apply {
+            moveTo(w * 0.3f, h * 0.62f)
+            lineTo(w * 0.5f, h * 0.72f)
+            lineTo(w * 0.5f, h * 0.62f)
+            lineTo(w * 0.3f, h * 0.52f)
+            close()
+        }
+        drawPath(pathInsideLeft, Color(0xFF047857).copy(0.3f))
+        
+        // Box inside-right face
+        val pathInsideRight = Path().apply {
+            moveTo(w * 0.5f, h * 0.72f)
+            lineTo(w * 0.7f, h * 0.62f)
+            lineTo(w * 0.7f, h * 0.52f)
+            lineTo(w * 0.5f, h * 0.62f)
+            close()
+        }
+        drawPath(pathInsideRight, Color(0xFF047857).copy(0.4f))
+        
+        // Flaps:
+        // Left flap (hanging out)
+        val pathLeftFlap = Path().apply {
+            moveTo(w * 0.3f, h * 0.62f)
+            lineTo(w * 0.18f, h * 0.58f)
+            lineTo(w * 0.38f, h * 0.68f)
+            lineTo(w * 0.5f, h * 0.72f)
+            close()
         }
+        drawPath(pathLeftFlap, Color(0xFFA7F3D0))
+        
+        // Right flap (hanging out)
+        val pathRightFlap = Path().apply {
+            moveTo(w * 0.7f, h * 0.62f)
+            lineTo(w * 0.82f, h * 0.58f)
+            lineTo(w * 0.62f, h * 0.68f)
+            lineTo(w * 0.5f, h * 0.72f)
+            close()
+        }
+        drawPath(pathRightFlap, Color(0xFF6EE7B7))
+        
+        // Back-left flap (open upright-ish)
+        val pathBackLeftFlap = Path().apply {
+            moveTo(w * 0.3f, h * 0.62f)
+            lineTo(w * 0.25f, h * 0.48f)
+            lineTo(w * 0.45f, h * 0.38f)
+            lineTo(w * 0.5f, h * 0.52f)
+            close()
+        }
+        drawPath(pathBackLeftFlap, Color(0xFF34D399).copy(0.8f))
+        
+        // Back-right flap (open upright-ish)
+        val pathBackRightFlap = Path().apply {
+            moveTo(w * 0.7f, h * 0.62f)
+            lineTo(w * 0.75f, h * 0.48f)
+            lineTo(w * 0.55f, h * 0.38f)
+            lineTo(w * 0.5f, h * 0.52f)
+            close()
+        }
+        drawPath(pathBackRightFlap, Color(0xFF34D399).copy(0.9f))
+        
+        // Paper airplane flying
+        val planePath = Path().apply {
+            moveTo(w * 0.62f, h * 0.35f) // nose
+            lineTo(w * 0.75f, h * 0.23f) // tail tip 1
+            lineTo(w * 0.72f, h * 0.38f) // middle fold tail
+            close()
+        }
+        drawPath(planePath, Color(0xFF34D399))
+        
+        val planeWingPath = Path().apply {
+            moveTo(w * 0.62f, h * 0.35f)
+            lineTo(w * 0.72f, h * 0.38f)
+            lineTo(w * 0.68f, h * 0.42f) // wing
+            close()
+        }
+        drawPath(planeWingPath, Color(0xFF10B981))
+        
+        // Small sparkles/stars around the airplane
+        drawCircle(Color(0xFF34D399), radius = 3.dp.toPx(), center = androidx.compose.ui.geometry.Offset(w * 0.8f, h * 0.2f))
+        drawCircle(Color(0xFFA7F3D0), radius = 2.dp.toPx(), center = androidx.compose.ui.geometry.Offset(w * 0.58f, h * 0.25f))
+        drawCircle(Color(0xFF6EE7B7), radius = 2.dp.toPx(), center = androidx.compose.ui.geometry.Offset(w * 0.72f, h * 0.5f))
     }
 }
 
-// ─── Payments ─────────────────────────────────────────────────────────────────
-
+@OptIn(ExperimentalMaterial3Api::class)
 @Composable
-fun DesktopPaymentsScreen(
-    viewModel: PaymentsViewModel = remember { inject() }
+fun DesktopOrdersScreen(
+    viewModel: OrdersViewModel = remember { inject() },
+    navigationViewModel: com.app.biashara.ui.DesktopNavigationViewModel = remember { inject() }
 ) {
     val state by viewModel.state.collectAsState()
     LaunchedEffect(Unit) {
-        viewModel.loadPayments()
+        viewModel.loadOrders()
     }
 
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
-        Row(horizontalArrangement = Arrangement.spacedBy(12.dp)) {
-            SummaryStatCard(Modifier.weight(1f), "Total Collected", "KES ${String.format("%,.0f", state.totalReconciled)}", B360Green)
-            SummaryStatCard(Modifier.weight(1f), "Unreconciled", "KES ${String.format("%,.0f", state.totalUnmatched)}", B360Amber)
-            SummaryStatCard(Modifier.weight(1f), "Mpesa Transactions", state.payments.size.toString(), B360Blue)
-            SummaryStatCard(Modifier.weight(1f), "Failed Payments", "0", B360Red)
-        }
-        Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(12.dp)) {
-            Column {
-                Row(Modifier.fillMaxWidth().background(Color(0xFFF8F8F8)).padding(16.dp, 12.dp)) {
-                    listOf("Mpesa Code", "Customer", "Phone", "Amount", "Channel", "Status", "Date", "").forEachIndexed { i, h ->
-                        Text(h, fontWeight = FontWeight.Bold, fontSize = 12.sp, color = Color.Gray, modifier = Modifier.weight(if (i == 7) 0.6f else 1f))
-                    }
-                }
-                HorizontalDivider()
-                if (state.payments.isEmpty()) {
-                    Text("No transactions recorded", color = Color.Gray, modifier = Modifier.padding(24.dp).align(Alignment.CenterHorizontally))
-                } else {
-                    state.payments.forEach { payment ->
-                        val statusColor = if (payment.reconciled) B360Green else B360Amber
-                        val statusText = if (payment.reconciled) "RECONCILED" else "PENDING"
-                        Row(Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 12.dp), verticalAlignment = Alignment.CenterVertically) {
-                            Text(payment.transactionCode, Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold, fontSize = 12.sp)
-                            Text(payment.payerName, Modifier.weight(1f), fontWeight = FontWeight.Medium)
-                            Text(payment.payerPhone, Modifier.weight(1f), color = Color.Gray, fontSize = 12.sp)
-                            Text("KES ${String.format("%,.0f", payment.amount)}", Modifier.weight(1f), fontWeight = FontWeight.Bold)
-                            Text("Mpesa", Modifier.weight(1f), fontSize = 12.sp)
-                            Surface(color = statusColor.copy(0.1f), shape = RoundedCornerShape(20.dp), modifier = Modifier.weight(1f)) {
-                                Text(statusText, color = statusColor, fontSize = 11.sp, modifier = Modifier.padding(horizontal = 8.dp, vertical = 3.dp))
-                            }
-                            Text(payment.transactionDate.toString().take(10), Modifier.weight(1f), color = Color.Gray, fontSize = 12.sp)
-                            if (!payment.reconciled) {
-                                TextButton(onClick = { viewModel.reconcilePayment(payment.id, payment.orderId ?: "") }, modifier = Modifier.weight(0.6f)) {
-                                    Text("Match", fontSize = 11.sp, color = B360Blue)
+    var searchQuery by remember { mutableStateOf("") }
+    val scrollState = rememberScrollState()
+    
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(16.dp)
+    ) {
+        // Title & Subtitle Row
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Orders",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Manage and track customer orders",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        // Filter Tabs & New Order Button Row
+        Row(
+            modifier = Modifier.fillMaxWidth(),
+            verticalAlignment = Alignment.CenterVertically,
+            horizontalArrangement = Arrangement.SpaceBetween
+        ) {
+            Row(horizontalArrangement = Arrangement.spacedBy(8.dp)) {
+                val selectedTab = state.selectedTabStatus
+                FilterTabChip(selected = selectedTab == null, onClick = { viewModel.selectTab(null) }, text = "All")
+                FilterTabChip(selected = selectedTab == PaymentStatus.PAID, onClick = { viewModel.selectTab(PaymentStatus.PAID) }, text = "Paid", icon = Icons.Filled.CheckCircle, iconColor = B360Green)
+                FilterTabChip(selected = selectedTab == PaymentStatus.PENDING, onClick = { viewModel.selectTab(PaymentStatus.PENDING) }, text = "Pending", icon = Icons.Filled.Schedule, iconColor = B360Amber)
+                FilterTabChip(selected = selectedTab == PaymentStatus.COD, onClick = { viewModel.selectTab(PaymentStatus.COD) }, text = "COD", icon = Icons.Filled.LocalShipping, iconColor = B360Blue)
+            }
+            
+            // New Order Button matching the screenshot
+            Button(
+                onClick = { navigationViewModel.navigateTo(com.app.biashara.ui.AppScreen.Pos) },
+                colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                shape = RoundedCornerShape(8.dp),
+                contentPadding = PaddingValues(horizontal = 16.dp, vertical = 10.dp)
+            ) {
+                Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(8.dp)) {
+                    Icon(Icons.Filled.Add, null, modifier = Modifier.size(18.dp))
+                    Text("New Order", fontWeight = FontWeight.Bold, fontSize = 14.sp)
+                    Box(
+                        modifier = Modifier
+                            .width(1.dp)
+                            .height(16.dp)
+                            .background(Color.White.copy(0.3f))
+                    )
+                    Icon(Icons.Filled.KeyboardArrowDown, null, modifier = Modifier.size(18.dp))
+                }
+            }
+        }
+
+        // Main content Card wrapping the search bar, headers, body, and pagination
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(modifier = Modifier.fillMaxWidth()) {
+                // Search, Filter, Date Range Row
+                Row(
+                    modifier = Modifier
+                        .fillMaxWidth()
+                        .padding(16.dp),
+                    horizontalArrangement = Arrangement.spacedBy(12.dp),
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    OutlinedTextField(
+                        value = searchQuery,
+                        onValueChange = { searchQuery = it },
+                        placeholder = { Text("Search orders by order #, customer or phone...", color = Color(0xFF94A3B8)) },
+                        leadingIcon = { Icon(Icons.Filled.Search, null, tint = Color(0xFF94A3B8), modifier = Modifier.size(18.dp)) },
+                        modifier = Modifier.weight(1f),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true,
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
+                    )
+                    
+                    OutlinedButton(
+                        onClick = {},
+                        shape = RoundedCornerShape(8.dp),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF1E293B)),
+                        contentPadding = PaddingValues(horizontal = 16.dp, vertical = 10.dp)
+                    ) {
+                        Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(6.dp)) {
+                            Icon(Icons.Filled.FilterList, null, modifier = Modifier.size(16.dp), tint = Color(0xFF64748B))
+                            Text("Filter", fontSize = 13.sp, fontWeight = FontWeight.SemiBold)
+                        }
+                    }
+                    
+                    OutlinedButton(
+                        onClick = {},
+                        shape = RoundedCornerShape(8.dp),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF1E293B)),
+                        contentPadding = PaddingValues(horizontal = 16.dp, vertical = 10.dp)
+                    ) {
+                        Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(6.dp)) {
+                            Icon(Icons.Filled.DateRange, null, modifier = Modifier.size(16.dp), tint = Color(0xFF64748B))
+                            Text("Date Range", fontSize = 13.sp, fontWeight = FontWeight.SemiBold)
+                            Icon(Icons.Filled.KeyboardArrowDown, null, modifier = Modifier.size(16.dp), tint = Color(0xFF64748B))
+                        }
+                    }
+                }
+
+                // Table Header Row with sort icons
+                Row(
+                    modifier = Modifier
+                        .fillMaxWidth()
+                        .background(Color(0xFFF8FAFC))
+                        .padding(horizontal = 16.dp, vertical = 12.dp),
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    val headers = listOf(
+                        Triple("Order #", 1f, Alignment.Start),
+                        Triple("Customer", 1.5f, Alignment.Start),
+                        Triple("Phone", 1.2f, Alignment.Start),
+                        Triple("Amount", 1.2f, Alignment.Start),
+                        Triple("Payment", 1.2f, Alignment.Start),
+                        Triple("Delivery", 1.2f, Alignment.Start),
+                        Triple("Date", 1.5f, Alignment.Start),
+                        Triple("Actions", 1f, Alignment.CenterHorizontally)
+                    )
+                    headers.forEach { (title, weight, alignment) ->
+                        Box(modifier = Modifier.weight(weight), contentAlignment = if (alignment == Alignment.Start) Alignment.CenterStart else Alignment.Center) {
+                            Row(
+                                verticalAlignment = Alignment.CenterVertically,
+                                horizontalArrangement = Arrangement.spacedBy(4.dp)
+                            ) {
+                                Text(
+                                    text = title,
+                                    fontWeight = FontWeight.Bold,
+                                    fontSize = 12.sp,
+                                    color = Color(0xFF475569)
+                                )
+                                if (title != "Actions") {
+                                    Icon(
+                                        imageVector = Icons.Filled.SwapVert,
+                                        contentDescription = "Sort",
+                                        tint = Color(0xFF94A3B8),
+                                        modifier = Modifier.size(14.dp)
+                                    )
+                                }
+                            }
+                        }
+                    }
+                }
+                
+                HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                val filteredOrdersList = state.filteredOrders.filter { order ->
+                    order.orderNumber.contains(searchQuery, ignoreCase = true) ||
+                    order.customerName.contains(searchQuery, ignoreCase = true) ||
+                    order.customerPhone.contains(searchQuery, ignoreCase = true)
+                }
+
+                if (filteredOrdersList.isEmpty()) {
+                    // Custom Empty State Illustration matching screenshot
+                    Column(
+                        modifier = Modifier
+                            .fillMaxWidth()
+                            .padding(vertical = 48.dp),
+                        horizontalAlignment = Alignment.CenterHorizontally,
+                        verticalArrangement = Arrangement.spacedBy(16.dp)
+                    ) {
+                        OrdersEmptyStateIllustration()
+                        
+                        Text(
+                            text = "No orders found",
+                            fontWeight = FontWeight.Bold,
+                            fontSize = 18.sp,
+                            color = Color(0xFF1E293B)
+                        )
+                        
+                        Text(
+                            text = "You don't have any orders yet. Create your first order to get started.",
+                            fontSize = 14.sp,
+                            color = Color(0xFF64748B),
+                            modifier = Modifier.padding(horizontal = 24.dp),
+                            textAlign = androidx.compose.ui.text.style.TextAlign.Center
+                        )
+                        
+                        OutlinedButton(
+                            onClick = { navigationViewModel.navigateTo(com.app.biashara.ui.AppScreen.Pos) },
+                            shape = RoundedCornerShape(8.dp),
+                            border = BorderStroke(1.5.dp, B360Green),
+                            colors = ButtonDefaults.outlinedButtonColors(contentColor = B360Green)
+                        ) {
+                            Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(6.dp)) {
+                                Icon(Icons.Filled.Add, null, modifier = Modifier.size(16.dp))
+                                Text("Create New Order", fontWeight = FontWeight.Bold)
+                            }
+                        }
+                    }
+                } else {
+                    filteredOrdersList.forEach { order ->
+                        val payColor = when (order.paymentStatus) {
+                            PaymentStatus.PAID -> B360Green
+                            PaymentStatus.PENDING -> B360Amber
+                            else -> B360Blue
+                        }
+                        val deliveryColor = when (order.deliveryStatus) {
+                            DeliveryStatus.DELIVERED -> B360Green
+                            DeliveryStatus.SHIPPED -> B360Blue
+                            DeliveryStatus.PENDING -> B360Amber
+                            else -> Color(0xFF64748B)
+                        }
+                        
+                        Row(
+                            modifier = Modifier
+                                .fillMaxWidth()
+                                .padding(horizontal = 16.dp, vertical = 12.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            Text(order.orderNumber, modifier = Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold, fontSize = 13.sp)
+                            Text(order.customerName, modifier = Modifier.weight(1.5f), fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF1E293B))
+                            Text(order.customerPhone, modifier = Modifier.weight(1.2f), color = Color(0xFF64748B), fontSize = 13.sp)
+                            Text("KES ${String.format("%,.0f", order.subtotal)}", modifier = Modifier.weight(1.2f), fontWeight = FontWeight.Bold, fontSize = 13.sp, color = Color(0xFF1E293B))
+                            
+                            Box(modifier = Modifier.weight(1.2f)) {
+                                Surface(
+                                    color = payColor.copy(0.1f),
+                                    shape = RoundedCornerShape(20.dp)
+                                ) {
+                                    Text(
+                                        text = order.paymentStatus.name,
+                                        color = payColor,
+                                        fontSize = 11.sp,
+                                        fontWeight = FontWeight.Bold,
+                                        modifier = Modifier.padding(horizontal = 10.dp, vertical = 4.dp)
+                                    )
+                                }
+                            }
+                            
+                            Box(modifier = Modifier.weight(1.2f)) {
+                                Surface(
+                                    color = deliveryColor.copy(0.1f),
+                                    shape = RoundedCornerShape(20.dp)
+                                ) {
+                                    Text(
+                                        text = order.deliveryStatus.name,
+                                        color = deliveryColor,
+                                        fontSize = 11.sp,
+                                        fontWeight = FontWeight.Bold,
+                                        modifier = Modifier.padding(horizontal = 10.dp, vertical = 4.dp)
+                                    )
+                                }
+                            }
+                            
+                            Text(order.createdAt.toString().take(16).replace("T", " "), modifier = Modifier.weight(1.5f), fontSize = 13.sp, color = Color(0xFF64748B))
+                            
+                            Row(
+                                modifier = Modifier.weight(1f),
+                                horizontalArrangement = Arrangement.Center,
+                                verticalAlignment = Alignment.CenterVertically
+                            ) {
+                                IconButton(onClick = {}, modifier = Modifier.size(28.dp)) {
+                                    Icon(Icons.Filled.Visibility, null, tint = B360Blue, modifier = Modifier.size(16.dp))
+                                }
+                                IconButton(onClick = { viewModel.cancelOrder(order.id) }, modifier = Modifier.size(28.dp)) {
+                                    Icon(Icons.Filled.Delete, null, tint = B360Red, modifier = Modifier.size(16.dp))
+                                }
+                            }
+                        }
+                        HorizontalDivider(color = Color(0xFFF1F5F9))
+                    }
+                }
+
+                // Table Pagination Footer matching the screenshot
+                Row(
+                    modifier = Modifier
+                        .fillMaxWidth()
+                        .padding(horizontal = 16.dp, vertical = 12.dp),
+                    horizontalArrangement = Arrangement.SpaceBetween,
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    Text(
+                        text = "Showing 0 to ${filteredOrdersList.size} of ${filteredOrdersList.size} orders",
+                        fontSize = 13.sp,
+                        color = Color(0xFF64748B)
+                    )
+                    
+                    Row(
+                        horizontalArrangement = Arrangement.spacedBy(8.dp),
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        OutlinedIconButton(
+                            onClick = {},
+                            modifier = Modifier.size(32.dp),
+                            shape = RoundedCornerShape(6.dp),
+                            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                        ) {
+                            Icon(Icons.Filled.KeyboardArrowLeft, null, modifier = Modifier.size(16.dp), tint = Color(0xFF64748B))
+                        }
+                        
+                        Surface(
+                            modifier = Modifier.size(32.dp),
+                            shape = RoundedCornerShape(6.dp),
+                            border = BorderStroke(1.5.dp, B360Green),
+                            color = Color.White
+                        ) {
+                            Box(contentAlignment = Alignment.Center) {
+                                Text("1", color = B360Green, fontWeight = FontWeight.Bold, fontSize = 13.sp)
+                            }
+                        }
+                        
+                        OutlinedIconButton(
+                            onClick = {},
+                            modifier = Modifier.size(32.dp),
+                            shape = RoundedCornerShape(6.dp),
+                            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                        ) {
+                            Icon(Icons.Filled.KeyboardArrowRight, null, modifier = Modifier.size(16.dp), tint = Color(0xFF64748B))
+                        }
+                    }
+                }
+            }
+        }
+    }
+}
+
+// Custom Composable for Filter Tab Chips to look premium
+@Composable
+fun FilterTabChip(
+    selected: Boolean,
+    onClick: () -> Unit,
+    text: String,
+    icon: ImageVector? = null,
+    iconColor: Color = Color.Unspecified
+) {
+    val containerBg = if (selected) Color(0xFFE6F7F0) else Color.White
+    val borderCol = if (selected) B360Green else Color(0xFFE2E8F0)
+    val textCol = if (selected) B360GreenDark else Color(0xFF475569)
+    
+    Surface(
+        onClick = onClick,
+        shape = RoundedCornerShape(8.dp),
+        border = BorderStroke(1.dp, borderCol),
+        color = containerBg,
+        modifier = Modifier.height(36.dp)
+    ) {
+        Row(
+            modifier = Modifier.padding(horizontal = 14.dp),
+            verticalAlignment = Alignment.CenterVertically,
+            horizontalArrangement = Arrangement.spacedBy(6.dp)
+        ) {
+            if (icon != null) {
+                Icon(icon, null, tint = if (selected) iconColor else Color(0xFF94A3B8), modifier = Modifier.size(16.dp))
+            }
+            Text(
+                text = text,
+                fontSize = 13.sp,
+                fontWeight = if (selected) FontWeight.Bold else FontWeight.Medium,
+                color = textCol
+            )
+        }
+    }
+}
+
+// ─── Customers ────────────────────────────────────────────────────────────────
+
+@Composable
+fun CustomersEmptyStateIllustration(modifier: Modifier = Modifier) {
+    Canvas(modifier = modifier.size(160.dp)) {
+        val w = size.width
+        val h = size.height
+        
+        val cardPath = Path().apply {
+            moveTo(w * 0.3f, h * 0.3f)
+            lineTo(w * 0.7f, h * 0.3f)
+            lineTo(w * 0.7f, h * 0.7f)
+            lineTo(w * 0.3f, h * 0.7f)
+            close()
+        }
+        drawPath(cardPath, Color(0xFFF1F5F9))
+        drawPath(cardPath, Color(0xFFCBD5E1), style = Stroke(width = 4f))
+        
+        drawCircle(Color(0xFFE2E8F0), radius = 18f, center = androidx.compose.ui.geometry.Offset(w * 0.5f, h * 0.45f))
+        
+        val shoulderPath = Path().apply {
+            moveTo(w * 0.42f, h * 0.62f)
+            quadraticBezierTo(w * 0.5f, h * 0.52f, w * 0.58f, h * 0.62f)
+        }
+        drawPath(shoulderPath, Color(0xFF94A3B8), style = Stroke(width = 4f))
+        
+        drawCircle(Color(0xFF10B981), radius = 5f, center = androidx.compose.ui.geometry.Offset(w * 0.25f, h * 0.25f))
+        drawCircle(Color(0xFF10B981).copy(0.4f), radius = 3f, center = androidx.compose.ui.geometry.Offset(w * 0.78f, h * 0.65f))
+    }
+}
+
+@Composable
+fun DesktopCustomersScreen(
+    viewModel: CustomersViewModel = remember { inject() }
+) {
+    val state by viewModel.state.collectAsState()
+    LaunchedEffect(Unit) {
+        viewModel.loadCustomers()
+    }
+    var showAddCustomerDialog by remember { mutableStateOf(false) }
+    var searchQuery by remember { mutableStateOf("") }
+    LaunchedEffect(searchQuery) {
+        viewModel.onSearchQueryChange(searchQuery)
+    }
+
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Customers",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Manage your client relationships and loyalty points",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+                Row(
+                    horizontalArrangement = Arrangement.spacedBy(12.dp),
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    OutlinedTextField(
+                        value = searchQuery,
+                        onValueChange = { searchQuery = it },
+                        placeholder = { Text("Search customers...", color = Color(0xFF94A3B8)) },
+                        leadingIcon = { Icon(Icons.Filled.Search, null, tint = Color(0xFF94A3B8), modifier = Modifier.size(18.dp)) },
+                        modifier = Modifier.weight(1f),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true,
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
+                    )
+                    OutlinedButton(
+                        onClick = {},
+                        shape = RoundedCornerShape(8.dp),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+                    ) {
+                        Icon(Icons.Filled.FilterList, null, Modifier.size(16.dp))
+                        Spacer(Modifier.width(6.dp))
+                        Text("Filter")
+                    }
+                    Button(
+                        onClick = { showAddCustomerDialog = true },
+                        shape = RoundedCornerShape(8.dp),
+                        colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                    ) {
+                        Icon(Icons.Filled.PersonAdd, null, Modifier.size(16.dp), tint = Color.White)
+                        Spacer(Modifier.width(6.dp))
+                        Text("Add Customer", color = Color.White)
+                    }
+                }
+
+                if (state.filteredCustomers.isEmpty()) {
+                    Column(
+                        modifier = Modifier.fillMaxWidth().padding(vertical = 48.dp),
+                        horizontalAlignment = Alignment.CenterHorizontally,
+                        verticalArrangement = Arrangement.spacedBy(16.dp)
+                    ) {
+                        CustomersEmptyStateIllustration()
+                        Text("No customers found", style = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold), color = Color(0xFF1E293B))
+                        Text("Build relationship networks by adding customer entries.", style = MaterialTheme.typography.bodyMedium, color = Color(0xFF64748B))
+                        Button(
+                            onClick = { showAddCustomerDialog = true },
+                            shape = RoundedCornerShape(8.dp),
+                            colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                        ) {
+                            Icon(Icons.Filled.PersonAdd, null, Modifier.size(16.dp), tint = Color.White)
+                            Spacer(Modifier.width(6.dp))
+                            Text("Add Customer", color = Color.White)
+                        }
+                    }
+                } else {
+                    Column {
+                        Row(
+                            Modifier
+                                .fillMaxWidth()
+                                .background(Color(0xFFF8FAFC))
+                                .padding(horizontal = 16.dp, vertical = 12.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            val headers = listOf("Customer", "Phone", "Orders", "Total Spent", "Loyalty Pts", "Actions")
+                            headers.forEachIndexed { i, header ->
+                                Row(
+                                    modifier = Modifier.weight(if (i == 0) 1.5f else 1f),
+                                    verticalAlignment = Alignment.CenterVertically,
+                                    horizontalArrangement = Arrangement.spacedBy(4.dp)
+                                ) {
+                                    Text(
+                                        text = header,
+                                        fontWeight = FontWeight.SemiBold,
+                                        fontSize = 13.sp,
+                                        color = Color(0xFF64748B)
+                                    )
+                                    if (header != "Actions") {
+                                        Icon(
+                                            imageVector = Icons.Filled.SwapVert,
+                                            contentDescription = "Sort",
+                                            tint = Color(0xFF94A3B8),
+                                            modifier = Modifier.size(14.dp)
+                                        )
+                                    }
+                                }
+                            }
+                        }
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                        state.filteredCustomers.forEach { customer ->
+                            Row(
+                                Modifier
+                                    .fillMaxWidth()
+                                    .padding(horizontal = 16.dp, vertical = 14.dp),
+                                verticalAlignment = Alignment.CenterVertically
+                            ) {
+                                Row(
+                                    Modifier.weight(1.5f),
+                                    horizontalArrangement = Arrangement.spacedBy(10.dp),
+                                    verticalAlignment = Alignment.CenterVertically
+                                ) {
+                                    Box(
+                                        Modifier
+                                            .size(32.dp)
+                                            .background(B360Green.copy(0.1f), RoundedCornerShape(50)),
+                                        contentAlignment = Alignment.Center
+                                    ) {
+                                        Text(
+                                            customer.name.firstOrNull()?.toString()?.uppercase() ?: "",
+                                            color = B360Green,
+                                            fontWeight = FontWeight.Bold
+                                        )
+                                    }
+                                    Text(customer.name, fontWeight = FontWeight.Medium, color = Color(0xFF1E293B))
+                                }
+                                Text(customer.phone, Modifier.weight(1f), color = Color(0xFF64748B), fontSize = 13.sp)
+                                Text("—", Modifier.weight(1f), fontWeight = FontWeight.SemiBold, color = Color(0xFF1E293B))
+                                Text("—", Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold)
+                                Row(
+                                    Modifier.weight(1f),
+                                    horizontalArrangement = Arrangement.spacedBy(4.dp),
+                                    verticalAlignment = Alignment.CenterVertically
+                                ) {
+                                    Icon(Icons.Filled.Star, null, tint = B360Amber, modifier = Modifier.size(14.dp))
+                                    Text(customer.loyaltyPoints.toString(), fontWeight = FontWeight.Medium, color = Color(0xFF1E293B))
                                 }
-                            } else Spacer(Modifier.weight(0.6f))
+                                Row(
+                                    Modifier.weight(1f),
+                                    horizontalArrangement = Arrangement.spacedBy(8.dp),
+                                    verticalAlignment = Alignment.CenterVertically
+                                ) {
+                                    IconButton(
+                                        onClick = {},
+                                        modifier = Modifier
+                                            .size(32.dp)
+                                            .border(1.dp, Color(0xFFE2E8F0), RoundedCornerShape(6.dp))
+                                    ) {
+                                        Icon(
+                                            imageVector = Icons.Filled.Visibility,
+                                            contentDescription = "View Details",
+                                            tint = B360Blue,
+                                            modifier = Modifier.size(16.dp)
+                                        )
+                                    }
+                                    IconButton(
+                                        onClick = {},
+                                        modifier = Modifier
+                                            .size(32.dp)
+                                            .border(1.dp, Color(0xFFE2E8F0), RoundedCornerShape(6.dp))
+                                    ) {
+                                        Icon(
+                                            imageVector = Icons.Filled.Chat,
+                                            contentDescription = "Message",
+                                            tint = B360Green,
+                                            modifier = Modifier.size(16.dp)
+                                        )
+                                    }
+                                }
+                            }
+                            HorizontalDivider(color = Color(0xFFF1F5F9))
+                        }
+                    }
+
+                    Row(
+                        modifier = Modifier.fillMaxWidth().padding(top = 12.dp),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(
+                            text = "Showing 1 to ${state.filteredCustomers.size} of ${state.filteredCustomers.size} customers",
+                            fontSize = 13.sp,
+                            color = Color(0xFF64748B)
+                        )
+                        Row(
+                            horizontalArrangement = Arrangement.spacedBy(6.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowLeft, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
+                            }
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, B360Green),
+                                colors = ButtonDefaults.outlinedButtonColors(containerColor = B360Green.copy(0.08f), contentColor = B360Green)
+                            ) {
+                                Text("1", fontWeight = FontWeight.Bold, fontSize = 13.sp)
+                            }
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowRight, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
+                            }
+                        }
+            }
+        }
+    }
+
+    if (showAddCustomerDialog) {
+        var name by remember { mutableStateOf("") }
+        var phone by remember { mutableStateOf("") }
+        var email by remember { mutableStateOf("") }
+        var error by remember { mutableStateOf<String?>(null) }
+
+        AlertDialog(
+            onDismissRequest = {
+                showAddCustomerDialog = false
+            },
+            title = {
+                Text(
+                    text = "Add New Customer",
+                    fontWeight = FontWeight.Bold,
+                    color = Color(0xFF1E293B)
+                )
+            },
+            text = {
+                Column(
+                    verticalArrangement = Arrangement.spacedBy(12.dp),
+                    modifier = Modifier.width(360.dp)
+                ) {
+                    if (error != null) {
+                        Text(error!!, color = B360Red, fontSize = 12.sp, fontWeight = FontWeight.Bold)
+                    }
+                    OutlinedTextField(
+                        value = name,
+                        onValueChange = { name = it },
+                        label = { Text("Customer Name *") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
+                    OutlinedTextField(
+                        value = phone,
+                        onValueChange = { phone = it },
+                        label = { Text("Phone Number *") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
+                    OutlinedTextField(
+                        value = email,
+                        onValueChange = { email = it },
+                        label = { Text("Email Address") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
+                }
+            },
+            confirmButton = {
+                Button(
+                    onClick = {
+                        if (name.isBlank() || phone.isBlank()) {
+                            error = "Name and Phone are required."
+                            return@Button
                         }
-                        HorizontalDivider(color = Color(0xFFF5F5F5))
+
+                        val customer = Customer(
+                            id = generateId(),
+                            businessId = UserSession.getBusinessId(),
+                            name = name,
+                            phone = phone,
+                            email = email,
+                            loyaltyPoints = 0,
+                            createdAt = Clock.System.now(),
+                            updatedAt = Clock.System.now()
+                        )
+                        viewModel.saveCustomer(customer)
+                        showAddCustomerDialog = false
+                    },
+                    colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                ) {
+                    Text("Save", color = Color.White)
+                }
+            },
+            dismissButton = {
+                TextButton(
+                    onClick = {
+                        showAddCustomerDialog = false
+                    }
+                ) {
+                    Text("Cancel", color = Color.Gray)
+                }
+            }
+        )
+    }
+}
+
+// ─── Expenses ─────────────────────────────────────────────────────────────────
+
+@Composable
+fun ExpensesEmptyStateIllustration(modifier: Modifier = Modifier) {
+    Canvas(modifier = modifier.size(160.dp)) {
+        val w = size.width
+        val h = size.height
+        
+        val receiptPath = Path().apply {
+            moveTo(w * 0.35f, h * 0.25f)
+            lineTo(w * 0.65f, h * 0.25f)
+            lineTo(w * 0.65f, h * 0.7f)
+            lineTo(w * 0.6f, h * 0.75f)
+            lineTo(w * 0.55f, h * 0.7f)
+            lineTo(w * 0.5f, h * 0.75f)
+            lineTo(w * 0.45f, h * 0.7f)
+            lineTo(w * 0.4f, h * 0.75f)
+            lineTo(w * 0.35f, h * 0.7f)
+            close()
+        }
+        drawPath(receiptPath, Color(0xFFF8FAFC))
+        drawPath(receiptPath, Color(0xFF94A3B8), style = Stroke(width = 4f))
+        
+        val line1 = Path().apply {
+            moveTo(w * 0.42f, h * 0.38f)
+            lineTo(w * 0.58f, h * 0.38f)
+        }
+        val line2 = Path().apply {
+            moveTo(w * 0.42f, h * 0.48f)
+            lineTo(w * 0.58f, h * 0.48f)
+        }
+        val line3 = Path().apply {
+            moveTo(w * 0.42f, h * 0.58f)
+            lineTo(w * 0.52f, h * 0.58f)
+        }
+        drawPath(line1, Color(0xFFCBD5E1), style = Stroke(width = 4f))
+        drawPath(line2, Color(0xFFCBD5E1), style = Stroke(width = 4f))
+        drawPath(line3, Color(0xFF10B981), style = Stroke(width = 4f))
+        
+        drawCircle(Color(0xFFFF8F00), radius = 5f, center = androidx.compose.ui.geometry.Offset(w * 0.25f, h * 0.4f))
+        drawCircle(Color(0xFF10B981), radius = 4f, center = androidx.compose.ui.geometry.Offset(w * 0.75f, h * 0.3f))
+    }
+}
+
+@Composable
+fun DesktopExpensesScreen(
+    viewModel: ExpensesViewModel = remember { inject() }
+) {
+    val state by viewModel.state.collectAsState()
+    LaunchedEffect(Unit) {
+        viewModel.loadExpenses()
+    }
+    var showAddExpenseDialog by remember { mutableStateOf(false) }
+
+    var searchQuery by remember { mutableStateOf("") }
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Expenses",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Track your business costs and outlays",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
+            SummaryStatCard(Modifier.weight(1f), "Total This Month", "KES ${String.format("%,.0f", state.totalAmount)}", B360Red)
+            SummaryStatCard(Modifier.weight(1f), "Advertising", "KES 8,500", B360Blue)
+            SummaryStatCard(Modifier.weight(1f), "Stock Purchase", "KES 45,000", B360Green)
+            SummaryStatCard(Modifier.weight(1f), "Operations", "KES 12,000", B360Amber)
+        }
+
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+                Row(
+                    horizontalArrangement = Arrangement.spacedBy(12.dp),
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    OutlinedTextField(
+                        value = searchQuery,
+                        onValueChange = { searchQuery = it },
+                        placeholder = { Text("Search expenses...", color = Color(0xFF94A3B8)) },
+                        leadingIcon = { Icon(Icons.Filled.Search, null, tint = Color(0xFF94A3B8), modifier = Modifier.size(18.dp)) },
+                        modifier = Modifier.weight(1f),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true,
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
+                    )
+                    OutlinedButton(
+                        onClick = {},
+                        shape = RoundedCornerShape(8.dp),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+                    ) {
+                        Icon(Icons.Filled.FilterList, null, Modifier.size(16.dp))
+                        Spacer(Modifier.width(6.dp))
+                        Text("Filter")
+                    }
+                    Button(
+                        onClick = { showAddExpenseDialog = true },
+                        shape = RoundedCornerShape(8.dp),
+                        colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                    ) {
+                        Icon(Icons.Filled.Add, null, Modifier.size(16.dp), tint = Color.White)
+                        Spacer(Modifier.width(6.dp))
+                        Text("Add Expense", color = Color.White)
+                    }
+                }
+
+                val filteredExpenses = state.expenses.filter {
+                    it.description.contains(searchQuery, ignoreCase = true) ||
+                    it.category.displayName().contains(searchQuery, ignoreCase = true)
+                }
+
+                if (filteredExpenses.isEmpty()) {
+                    Column(
+                        modifier = Modifier.fillMaxWidth().padding(vertical = 48.dp),
+                        horizontalAlignment = Alignment.CenterHorizontally,
+                        verticalArrangement = Arrangement.spacedBy(16.dp)
+                    ) {
+                        ExpensesEmptyStateIllustration()
+                        Text("No expenses recorded", style = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold), color = Color(0xFF1E293B))
+                        Text("Keep track of your cash flows by recording business expenses.", style = MaterialTheme.typography.bodyMedium, color = Color(0xFF64748B))
+                        Button(
+                            onClick = { showAddExpenseDialog = true },
+                            shape = RoundedCornerShape(8.dp),
+                            colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                        ) {
+                            Icon(Icons.Filled.Add, null, Modifier.size(16.dp), tint = Color.White)
+                            Spacer(Modifier.width(6.dp))
+                            Text("Add Expense", color = Color.White)
+                        }
+                    }
+                } else {
+                    Column {
+                        Row(
+                            Modifier
+                                .fillMaxWidth()
+                                .background(Color(0xFFF8FAFC))
+                                .padding(horizontal = 16.dp, vertical = 12.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            val headers = listOf("Description", "Category", "Amount", "Date", "Actions")
+                            headers.forEachIndexed { i, header ->
+                                Row(
+                                    modifier = Modifier.weight(if (i == 0) 2f else 1f),
+                                    verticalAlignment = Alignment.CenterVertically,
+                                    horizontalArrangement = Arrangement.spacedBy(4.dp)
+                                ) {
+                                    Text(
+                                        text = header,
+                                        fontWeight = FontWeight.SemiBold,
+                                        fontSize = 13.sp,
+                                        color = Color(0xFF64748B)
+                                    )
+                                    if (header != "Actions") {
+                                        Icon(
+                                            imageVector = Icons.Filled.SwapVert,
+                                            contentDescription = "Sort",
+                                            tint = Color(0xFF94A3B8),
+                                            modifier = Modifier.size(14.dp)
+                                        )
+                                    }
+                                }
+                            }
+                        }
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                        filteredExpenses.forEach { expense ->
+                            val catColor = when (expense.category) {
+                                ExpenseCategory.ADVERTISING -> B360Blue
+                                ExpenseCategory.RENT -> B360Red
+                                ExpenseCategory.STOCK_PURCHASE -> B360Green
+                                ExpenseCategory.DELIVERY -> B360Amber
+                                else -> Color.Gray
+                            }
+                            Row(
+                                Modifier
+                                    .fillMaxWidth()
+                                    .padding(horizontal = 16.dp, vertical = 14.dp),
+                                verticalAlignment = Alignment.CenterVertically
+                            ) {
+                                Text(expense.description, Modifier.weight(2f), fontWeight = FontWeight.Medium, color = Color(0xFF1E293B))
+                                Box(modifier = Modifier.weight(1f)) {
+                                    Surface(color = catColor.copy(0.1f), shape = RoundedCornerShape(20.dp)) {
+                                        Text(
+                                            expense.category.displayName(),
+                                            color = catColor,
+                                            fontSize = 11.sp,
+                                            fontWeight = FontWeight.SemiBold,
+                                            modifier = Modifier.padding(horizontal = 10.dp, vertical = 4.dp)
+                                        )
+                                    }
+                                }
+                                Text("KES ${String.format("%,.0f", expense.amount)}", Modifier.weight(1f), color = B360Red, fontWeight = FontWeight.SemiBold)
+                                Text(expense.expenseDate.toString().take(10), Modifier.weight(1f), color = Color(0xFF64748B), fontSize = 13.sp)
+                                Row(Modifier.weight(1f)) {
+                                    IconButton(
+                                        onClick = { viewModel.deleteExpense(expense.id) },
+                                        modifier = Modifier
+                                            .size(32.dp)
+                                            .border(1.dp, Color(0xFFE2E8F0), RoundedCornerShape(6.dp))
+                                    ) {
+                                        Icon(
+                                            Icons.Filled.Delete,
+                                            null,
+                                            tint = B360Red,
+                                            modifier = Modifier.size(16.dp)
+                                        )
+                                    }
+                                }
+                            }
+                            HorizontalDivider(color = Color(0xFFF1F5F9))
+                        }
+                    }
+
+                    Row(
+                        modifier = Modifier.fillMaxWidth().padding(top = 12.dp),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(
+                            text = "Showing 1 to ${filteredExpenses.size} of ${filteredExpenses.size} expenses",
+                            fontSize = 13.sp,
+                            color = Color(0xFF64748B)
+                        )
+                        Row(
+                            horizontalArrangement = Arrangement.spacedBy(6.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowLeft, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
+                            }
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, B360Green),
+                                colors = ButtonDefaults.outlinedButtonColors(containerColor = B360Green.copy(0.08f), contentColor = B360Green)
+                            ) {
+                                Text("1", fontWeight = FontWeight.Bold, fontSize = 13.sp)
+                            }
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowRight, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
+                            }
+                        }
+            }
+        }
+    }
+
+    if (showAddExpenseDialog) {
+        var description by remember { mutableStateOf("") }
+        var amount by remember { mutableStateOf("") }
+        var selectedCategory by remember { mutableStateOf(ExpenseCategory.ADVERTISING) }
+        var expandedCategoryMenu by remember { mutableStateOf(false) }
+        var error by remember { mutableStateOf<String?>(null) }
+
+        AlertDialog(
+            onDismissRequest = {
+                showAddExpenseDialog = false
+            },
+            title = {
+                Text(
+                    text = "Add New Expense",
+                    fontWeight = FontWeight.Bold,
+                    color = Color(0xFF1E293B)
+                )
+            },
+            text = {
+                Column(
+                    verticalArrangement = Arrangement.spacedBy(12.dp),
+                    modifier = Modifier.width(360.dp)
+                ) {
+                    if (error != null) {
+                        Text(error!!, color = B360Red, fontSize = 12.sp, fontWeight = FontWeight.Bold)
+                    }
+                    OutlinedTextField(
+                        value = description,
+                        onValueChange = { description = it },
+                        label = { Text("Description *") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
+                    OutlinedTextField(
+                        value = amount,
+                        onValueChange = { amount = it },
+                        label = { Text("Amount (KES) *") },
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true
+                    )
+                    
+                    Text("Category", fontWeight = FontWeight.SemiBold, fontSize = 14.sp)
+                    Box(modifier = Modifier.fillMaxWidth()) {
+                        OutlinedButton(
+                            onClick = { expandedCategoryMenu = true },
+                            modifier = Modifier.fillMaxWidth(),
+                            shape = RoundedCornerShape(8.dp),
+                            colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF1E293B))
+                        ) {
+                            Row(
+                                modifier = Modifier.fillMaxWidth(),
+                                horizontalArrangement = Arrangement.SpaceBetween,
+                                verticalAlignment = Alignment.CenterVertically
+                            ) {
+                                Text(selectedCategory.displayName())
+                                Icon(Icons.Filled.ArrowDropDown, null)
+                            }
+                        }
+                        DropdownMenu(
+                            expanded = expandedCategoryMenu,
+                            onDismissRequest = { expandedCategoryMenu = false },
+                            modifier = Modifier.width(360.dp)
+                        ) {
+                            ExpenseCategory.entries.forEach { category ->
+                                DropdownMenuItem(
+                                    text = { Text(category.displayName()) },
+                                    onClick = {
+                                        selectedCategory = category
+                                        expandedCategoryMenu = false
+                                    }
+                                )
+                            }
+                        }
+                    }
+                }
+            },
+            confirmButton = {
+                Button(
+                    onClick = {
+                        val amtValue = amount.toDoubleOrNull()
+                        if (description.isBlank() || amtValue == null || amtValue <= 0) {
+                            error = "Please fill in all fields with valid values."
+                            return@Button
+                        }
+
+                        val now = Clock.System.now()
+                        val today = now.toLocalDateTime(TimeZone.of("Africa/Nairobi")).date
+
+                        val expense = Expense(
+                            id = generateId(),
+                            businessId = UserSession.getBusinessId(),
+                            category = selectedCategory,
+                            amount = amtValue,
+                            description = description,
+                            recordedAt = now,
+                            expenseDate = today
+                        )
+                        viewModel.saveExpense(expense)
+                        showAddExpenseDialog = false
+                    },
+                    colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                ) {
+                    Text("Save", color = Color.White)
+                }
+            },
+            dismissButton = {
+                TextButton(
+                    onClick = {
+                        showAddExpenseDialog = false
+                    }
+                ) {
+                    Text("Cancel", color = Color.Gray)
+                }
+            }
+        )
+    }
+}
+
+@Composable
+fun SummaryStatCard(modifier: Modifier, label: String, value: String, color: Color) {
+    Card(
+        modifier = modifier,
+        shape = RoundedCornerShape(12.dp),
+        colors = CardDefaults.cardColors(containerColor = Color.White),
+        border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+    ) {
+        Row(
+            modifier = Modifier.padding(20.dp),
+            horizontalArrangement = Arrangement.spacedBy(16.dp),
+            verticalAlignment = Alignment.CenterVertically
+        ) {
+            Box(
+                modifier = Modifier
+                    .size(40.dp)
+                    .clip(CircleShape)
+                    .background(color.copy(alpha = 0.1f)),
+                contentAlignment = Alignment.Center
+            ) {
+                Icon(
+                    imageVector = when (label) {
+                        "Total This Month", "Total Collected" -> Icons.Filled.Payments
+                        "Advertising" -> Icons.Filled.Campaign
+                        "Stock Purchase" -> Icons.Filled.Inventory
+                        "Unreconciled" -> Icons.Filled.Pending
+                        "Mpesa Transactions" -> Icons.Filled.ReceiptLong
+                        "Failed Payments" -> Icons.Filled.ErrorOutline
+                        else -> Icons.Filled.AccountBalanceWallet
+                    },
+                    contentDescription = null,
+                    tint = color,
+                    modifier = Modifier.size(20.dp)
+                )
+            }
+            Column {
+                Text(label, fontSize = 13.sp, color = Color(0xFF64748B), fontWeight = FontWeight.Medium)
+                Spacer(Modifier.height(2.dp))
+                Text(value, fontWeight = FontWeight.Bold, fontSize = 20.sp, color = Color(0xFF1E293B))
+            }
+        }
+    }
+}
+
+// ─── Payments ─────────────────────────────────────────────────────────────────
+
+@Composable
+fun PaymentsEmptyStateIllustration(modifier: Modifier = Modifier) {
+    Canvas(modifier = modifier.size(160.dp)) {
+        val w = size.width
+        val h = size.height
+        
+        val cardPath = Path().apply {
+            moveTo(w * 0.25f, h * 0.35f)
+            lineTo(w * 0.75f, h * 0.35f)
+            lineTo(w * 0.75f, h * 0.65f)
+            lineTo(w * 0.25f, h * 0.65f)
+            close()
+        }
+        drawPath(cardPath, Color(0xFFF8FAFC))
+        drawPath(cardPath, Color(0xFF94A3B8), style = Stroke(width = 4f))
+        
+        val stripePath = Path().apply {
+            moveTo(w * 0.25f, h * 0.42f)
+            lineTo(w * 0.75f, h * 0.42f)
+        }
+        drawPath(stripePath, Color(0xFF475569), style = Stroke(width = 8f))
+        
+        val chipPath = Path().apply {
+            moveTo(w * 0.32f, h * 0.52f)
+            lineTo(w * 0.42f, h * 0.52f)
+            lineTo(w * 0.42f, h * 0.6f)
+            lineTo(w * 0.32f, h * 0.6f)
+            close()
+        }
+        drawPath(chipPath, Color(0xFFF59E0B))
+        
+        drawCircle(Color(0xFF10B981), radius = 5f, center = androidx.compose.ui.geometry.Offset(w * 0.8f, h * 0.28f))
+        drawCircle(Color(0xFF10B981).copy(0.4f), radius = 3f, center = androidx.compose.ui.geometry.Offset(w * 0.15f, h * 0.55f))
+    }
+}
+
+@Composable
+fun DesktopPaymentsScreen(
+    viewModel: PaymentsViewModel = remember { inject() }
+) {
+    val state by viewModel.state.collectAsState()
+    LaunchedEffect(Unit) {
+        viewModel.loadPayments()
+    }
+
+    var searchQuery by remember { mutableStateOf("") }
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Payments",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Monitor and reconcile customer payments",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
+            SummaryStatCard(Modifier.weight(1f), "Total Collected", "KES ${String.format("%,.0f", state.totalReconciled)}", B360Green)
+            SummaryStatCard(Modifier.weight(1f), "Unreconciled", "KES ${String.format("%,.0f", state.totalUnmatched)}", B360Amber)
+            SummaryStatCard(Modifier.weight(1f), "Mpesa Transactions", state.payments.size.toString(), B360Blue)
+            SummaryStatCard(Modifier.weight(1f), "Failed Payments", "0", B360Red)
+        }
+
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+                Row(
+                    horizontalArrangement = Arrangement.spacedBy(12.dp),
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    OutlinedTextField(
+                        value = searchQuery,
+                        onValueChange = { searchQuery = it },
+                        placeholder = { Text("Search transactions...", color = Color(0xFF94A3B8)) },
+                        leadingIcon = { Icon(Icons.Filled.Search, null, tint = Color(0xFF94A3B8), modifier = Modifier.size(18.dp)) },
+                        modifier = Modifier.weight(1f),
+                        shape = RoundedCornerShape(8.dp),
+                        singleLine = true,
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
+                    )
+                    OutlinedButton(
+                        onClick = {},
+                        shape = RoundedCornerShape(8.dp),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+                    ) {
+                        Icon(Icons.Filled.FilterList, null, Modifier.size(16.dp))
+                        Spacer(Modifier.width(6.dp))
+                        Text("Filter")
+                    }
+                }
+
+                val filteredPayments = state.payments.filter {
+                    it.transactionCode.contains(searchQuery, ignoreCase = true) ||
+                    it.payerName.contains(searchQuery, ignoreCase = true) ||
+                    it.payerPhone.contains(searchQuery, ignoreCase = true)
+                }
+
+                if (filteredPayments.isEmpty()) {
+                    Column(
+                        modifier = Modifier.fillMaxWidth().padding(vertical = 48.dp),
+                        horizontalAlignment = Alignment.CenterHorizontally,
+                        verticalArrangement = Arrangement.spacedBy(16.dp)
+                    ) {
+                        PaymentsEmptyStateIllustration()
+                        Text("No transactions found", style = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold), color = Color(0xFF1E293B))
+                        Text("Reconcile payment logs matching Mpesa transaction codes.", style = MaterialTheme.typography.bodyMedium, color = Color(0xFF64748B))
+                    }
+                } else {
+                    Column {
+                        Row(
+                            Modifier
+                                .fillMaxWidth()
+                                .background(Color(0xFFF8FAFC))
+                                .padding(horizontal = 16.dp, vertical = 12.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            val headers = listOf("Mpesa Code", "Customer", "Phone", "Amount", "Channel", "Status", "Date", "Actions")
+                            headers.forEachIndexed { i, header ->
+                                Row(
+                                    modifier = Modifier.weight(if (header == "Actions") 0.6f else 1f),
+                                    verticalAlignment = Alignment.CenterVertically,
+                                    horizontalArrangement = Arrangement.spacedBy(4.dp)
+                                ) {
+                                    Text(
+                                        text = header,
+                                        fontWeight = FontWeight.SemiBold,
+                                        fontSize = 13.sp,
+                                        color = Color(0xFF64748B)
+                                    )
+                                    if (header != "Actions") {
+                                        Icon(
+                                            imageVector = Icons.Filled.SwapVert,
+                                            contentDescription = "Sort",
+                                            tint = Color(0xFF94A3B8),
+                                            modifier = Modifier.size(14.dp)
+                                        )
+                                    }
+                                }
+                            }
+                        }
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                        filteredPayments.forEach { payment ->
+                            val statusColor = if (payment.reconciled) B360Green else B360Amber
+                            val statusText = if (payment.reconciled) "Reconciled" else "Pending"
+                            Row(
+                                Modifier
+                                    .fillMaxWidth()
+                                    .padding(horizontal = 16.dp, vertical = 14.dp),
+                                verticalAlignment = Alignment.CenterVertically
+                            ) {
+                                Text(payment.transactionCode, Modifier.weight(1f), color = B360Green, fontWeight = FontWeight.SemiBold, fontSize = 12.sp)
+                                Text(payment.payerName, Modifier.weight(1f), fontWeight = FontWeight.Medium, color = Color(0xFF1E293B))
+                                Text(payment.payerPhone, Modifier.weight(1f), color = Color(0xFF64748B), fontSize = 12.sp)
+                                Text("KES ${String.format("%,.0f", payment.amount)}", Modifier.weight(1f), fontWeight = FontWeight.Bold, color = Color(0xFF1E293B))
+                                Text("Mpesa", Modifier.weight(1f), fontSize = 12.sp, color = Color(0xFF64748B))
+                                Box(modifier = Modifier.weight(1f)) {
+                                    Surface(color = statusColor.copy(0.1f), shape = RoundedCornerShape(20.dp)) {
+                                        Text(
+                                            statusText,
+                                            color = statusColor,
+                                            fontSize = 11.sp,
+                                            fontWeight = FontWeight.SemiBold,
+                                            modifier = Modifier.padding(horizontal = 10.dp, vertical = 4.dp)
+                                        )
+                                    }
+                                }
+                                Text(payment.transactionDate.toString().take(10), Modifier.weight(1f), color = Color(0xFF64748B), fontSize = 12.sp)
+                                Row(Modifier.weight(0.6f)) {
+                                    if (!payment.reconciled) {
+                                        OutlinedButton(
+                                            onClick = { viewModel.reconcilePayment(payment.id, payment.orderId ?: "") },
+                                            modifier = Modifier.height(28.dp),
+                                            contentPadding = PaddingValues(horizontal = 8.dp, vertical = 0.dp),
+                                            shape = RoundedCornerShape(6.dp),
+                                            border = BorderStroke(1.dp, B360Blue),
+                                            colors = ButtonDefaults.outlinedButtonColors(contentColor = B360Blue)
+                                        ) {
+                                            Text("Match", fontSize = 11.sp)
+                                        }
+                                    } else {
+                                        Spacer(Modifier.fillMaxWidth())
+                                    }
+                                }
+                            }
+                            HorizontalDivider(color = Color(0xFFF1F5F9))
+                        }
+                    }
+
+                    Row(
+                        modifier = Modifier.fillMaxWidth().padding(top = 12.dp),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(
+                            text = "Showing 1 to ${filteredPayments.size} of ${filteredPayments.size} transactions",
+                            fontSize = 13.sp,
+                            color = Color(0xFF64748B)
+                        )
+                        Row(
+                            horizontalArrangement = Arrangement.spacedBy(6.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowLeft, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
+                            }
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, B360Green),
+                                colors = ButtonDefaults.outlinedButtonColors(containerColor = B360Green.copy(0.08f), contentColor = B360Green)
+                            ) {
+                                Text("1", fontWeight = FontWeight.Bold, fontSize = 13.sp)
+                            }
+                            OutlinedButton(
+                                onClick = {},
+                                modifier = Modifier.size(36.dp),
+                                contentPadding = PaddingValues(0.dp),
+                                shape = RoundedCornerShape(8.dp),
+                                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                            ) {
+                                Icon(Icons.Filled.KeyboardArrowRight, null, modifier = Modifier.size(18.dp), tint = Color(0xFF64748B))
+                            }
+                        }
+                    }
+                }
+            }
+        }
+    }
+}
+
+// ─── Reports ──────────────────────────────────────────────────────────────────
+
+@Composable
+fun DesktopReportsScreen(
+    viewModel: ReportsViewModel = remember { inject() }
+) {
+    val state by viewModel.state.collectAsState()
+    LaunchedEffect(Unit) {
+        viewModel.loadReport("This Month")
+    }
+
+    val summary = state.profitSummary
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Reports",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Analyze your financial performance and margins",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        Row(
+            modifier = Modifier.fillMaxWidth(),
+            horizontalArrangement = Arrangement.spacedBy(16.dp)
+        ) {
+            // P&L Summary Card
+            Card(
+                modifier = Modifier.weight(1f),
+                shape = RoundedCornerShape(12.dp),
+                colors = CardDefaults.cardColors(containerColor = Color.White),
+                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+            ) {
+                Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
+                    Text(
+                        text = "Profit & Loss — March 2025",
+                        fontWeight = FontWeight.Bold,
+                        style = MaterialTheme.typography.titleMedium,
+                        color = Color(0xFF1E293B)
+                    )
+                    HorizontalDivider(color = Color(0xFFE2E8F0))
+                    if (summary == null) {
+                        Text(
+                            text = "Loading profit summary...",
+                            color = Color(0xFF64748B),
+                            modifier = Modifier.padding(vertical = 12.dp)
+                        )
+                    } else {
+                        PnlRow("Total Revenue", "KES ${String.format("%,.0f", summary.totalRevenue)}", B360Green)
+                        PnlRow("Cost of Goods Sold", "KES ${String.format("%,.0f", summary.totalCostOfGoods)}", B360Red)
+                        PnlRow("Gross Profit", "KES ${String.format("%,.0f", summary.grossProfit)}", B360Green, bold = true)
+                        PnlRow("Total Expenses", "KES ${String.format("%,.0f", summary.totalExpenses)}", B360Red)
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
+                        PnlRow("Net Profit", "KES ${String.format("%,.0f", summary.netProfit)}", B360Green, bold = true, large = true)
+                        PnlRow("Net Margin", "${String.format("%.1f", summary.netMargin * 100)}%", B360Blue)
+                    }
+                }
+            }
+
+            // Expense Breakdown Card
+            Card(
+                modifier = Modifier.weight(1f),
+                shape = RoundedCornerShape(12.dp),
+                colors = CardDefaults.cardColors(containerColor = Color.White),
+                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+            ) {
+                Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
+                    Text(
+                        text = "Expense Breakdown",
+                        fontWeight = FontWeight.Bold,
+                        style = MaterialTheme.typography.titleMedium,
+                        color = Color(0xFF1E293B)
+                    )
+                    HorizontalDivider(color = Color(0xFFE2E8F0))
+                    val items = listOf(
+                        Triple("Stock Purchase", "KES 45,000", 0.69f),
+                        Triple("Advertising", "KES 8,500", 0.13f),
+                        Triple("Rent", "KES 15,000", 0.23f),
+                        Triple("Delivery", "KES 3,200", 0.05f),
+                        Triple("Packaging", "KES 1,200", 0.02f)
+                    )
+                    items.forEach { (cat, amt, frac) ->
+                        Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                            Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween) {
+                                Text(cat, fontSize = 13.sp, color = Color(0xFF475569), fontWeight = FontWeight.Medium)
+                                Text(amt, fontWeight = FontWeight.SemiBold, fontSize = 13.sp, color = Color(0xFF1E293B))
+                            }
+                            LinearProgressIndicator(
+                                progress = frac,
+                                modifier = Modifier
+                                    .fillMaxWidth()
+                                    .height(8.dp)
+                                    .clip(RoundedCornerShape(4.dp)),
+                                color = B360Green,
+                                trackColor = B360Green.copy(0.12f)
+                            )
+                        }
+                    }
+                }
+            }
+        }
+
+        // Export Actions
+        Row(horizontalArrangement = Arrangement.spacedBy(12.dp)) {
+            OutlinedButton(
+                onClick = {},
+                shape = RoundedCornerShape(8.dp),
+                border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+            ) {
+                Icon(Icons.Filled.PictureAsPdf, null, Modifier.size(16.dp))
+                Spacer(Modifier.width(6.dp))
+                Text("Export PDF")
+            }
+            OutlinedButton(
+                onClick = {},
+                shape = RoundedCornerShape(8.dp),
+                border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+            ) {
+                Icon(Icons.Filled.TableChart, null, Modifier.size(16.dp))
+                Spacer(Modifier.width(6.dp))
+                Text("Export Excel")
+            }
+            OutlinedButton(
+                onClick = {},
+                shape = RoundedCornerShape(8.dp),
+                border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+            ) {
+                Icon(Icons.Filled.Share, null, Modifier.size(16.dp))
+                Spacer(Modifier.width(6.dp))
+                Text("Share via WhatsApp")
+            }
+        }
+    }
+}
+
+@Composable
+fun PnlRow(label: String, value: String, color: Color, bold: Boolean = false, large: Boolean = false) {
+    Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, verticalAlignment = Alignment.CenterVertically) {
+        Text(
+            text = label,
+            fontWeight = if (bold) FontWeight.Bold else FontWeight.Normal,
+            fontSize = if (large) 16.sp else 13.sp,
+            color = if (bold) Color(0xFF1E293B) else Color(0xFF475569)
+        )
+        Text(
+            text = value,
+            color = color,
+            fontWeight = if (bold) FontWeight.Bold else FontWeight.SemiBold,
+            fontSize = if (large) 16.sp else 13.sp
+        )
+    }
+}
+
+// ─── Settings ─────────────────────────────────────────────────────────────────
+
+@Composable
+fun DesktopSettingsScreen() {
+    val scrollState = rememberScrollState()
+    var backendUrl by remember { mutableStateOf(com.app.biashara.data.remote.BASE_URL) }
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Settings",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Configure your store settings and preferences",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        SettingsSection("Business Profile") {
+            SettingsField("Business Name", "Wanjiru's Fashion")
+            SettingsField("Owner Phone", "+254712345678")
+            SettingsField("Business Type", "Retail")
+            SettingsField("Mpesa Short Code", "174379")
+        }
+        SettingsSection("Security") {
+            SettingsToggle("Two-Factor Authentication (2FA)", true)
+            SettingsToggle("Email Notifications", true)
+            SettingsToggle("SMS Alerts", false)
+        }
+        SettingsSection("Subscription") {
+            Row(
+                Modifier.fillMaxWidth(),
+                horizontalArrangement = Arrangement.SpaceBetween,
+                verticalAlignment = Alignment.CenterVertically
+            ) {
+                Column {
+                    Text("Current Plan", fontWeight = FontWeight.Bold, color = Color(0xFF1E293B), fontSize = 14.sp)
+                    Spacer(Modifier.height(2.dp))
+                    Text("Freemium", color = Color(0xFF64748B), fontSize = 13.sp)
+                }
+                Button(
+                    onClick = {},
+                    shape = RoundedCornerShape(8.dp),
+                    colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+                ) {
+                    Text("Upgrade to Premium", color = Color.White)
+                }
+            }
+        }
+        SettingsSection("Backend Connectivity") {
+            OutlinedTextField(
+                value = backendUrl,
+                onValueChange = { backendUrl = it },
+                label = { Text("Backend URL") },
+                singleLine = true,
+                shape = RoundedCornerShape(8.dp),
+                modifier = Modifier.fillMaxWidth(),
+                colors = OutlinedTextFieldDefaults.colors(
+                    focusedBorderColor = B360Green,
+                    unfocusedBorderColor = Color(0xFFE2E8F0),
+                    focusedContainerColor = Color.White,
+                    unfocusedContainerColor = Color.White
+                )
+            )
+            Spacer(Modifier.height(4.dp))
+            Button(
+                onClick = {
+                    val configDir = File(System.getProperty("user.home"), ".biashara360")
+                    if (!configDir.exists()) configDir.mkdirs()
+                    val configFile = File(configDir, "base_url.txt")
+                    configFile.writeText(backendUrl.trim())
+                    com.app.biashara.data.remote.BASE_URL = backendUrl.trim()
+                },
+                shape = RoundedCornerShape(8.dp),
+                colors = ButtonDefaults.buttonColors(containerColor = B360Green)
+            ) {
+                Text("Save Backend URL", color = Color.White)
+            }
+        }
+    }
+}
+
+@Composable
+fun SettingsSection(title: String, content: @Composable ColumnScope.() -> Unit) {
+    Card(
+        Modifier.fillMaxWidth(),
+        shape = RoundedCornerShape(12.dp),
+        colors = CardDefaults.cardColors(containerColor = Color.White),
+        border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+    ) {
+        Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+            Text(
+                title,
+                fontWeight = FontWeight.Bold,
+                style = MaterialTheme.typography.titleMedium,
+                color = Color(0xFF1E293B)
+            )
+            HorizontalDivider(color = Color(0xFFE2E8F0))
+            content()
+        }
+    }
+}
+
+@Composable
+fun SettingsField(label: String, value: String) {
+    var textVal by remember { mutableStateOf(value) }
+    Row(
+        Modifier.fillMaxWidth(),
+        horizontalArrangement = Arrangement.SpaceBetween,
+        Alignment.CenterVertically
+    ) {
+        Text(label, color = Color(0xFF475569), fontWeight = FontWeight.Medium, fontSize = 14.sp)
+        OutlinedTextField(
+            value = textVal,
+            onValueChange = { textVal = it },
+            modifier = Modifier.width(320.dp),
+            shape = RoundedCornerShape(8.dp),
+            singleLine = true,
+            colors = OutlinedTextFieldDefaults.colors(
+                focusedBorderColor = B360Green,
+                unfocusedBorderColor = Color(0xFFE2E8F0),
+                focusedContainerColor = Color.White,
+                unfocusedContainerColor = Color.White
+            )
+        )
+    }
+}
+
+@Composable
+fun SettingsToggle(label: String, checked: Boolean) {
+    var state by remember { mutableStateOf(checked) }
+    Row(
+        Modifier.fillMaxWidth(),
+        horizontalArrangement = Arrangement.SpaceBetween,
+        Alignment.CenterVertically
+    ) {
+        Text(label, color = Color(0xFF1E293B), fontWeight = FontWeight.Medium, fontSize = 14.sp)
+        Switch(
+            checked = state,
+            onCheckedChange = { state = it },
+            colors = SwitchDefaults.colors(
+                checkedThumbColor = Color.White,
+                checkedTrackColor = B360Green
+            )
+        )
+    }
+}
+
+// ── Tax Screen ────────────────────────────────────────────────────────────────
+@Composable
+fun DesktopTaxScreen() {
+    val taxTypes = listOf(
+        Triple("VAT 16%", "KES 48,000", "Due 20th"),
+        Triple("TOT 1.5%", "KES 4,500", "Due 20th"),
+        Triple("WHT 3%", "KES 9,000", "Due 20th"),
+    )
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "Tax Management",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Track your business tax liability and due dates",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
+            listOf("VAT Liability" to "KES 48,000" to B360Red, "TOT Liability" to "KES 4,500" to B360Amber, "Next Filing" to "Mar 20" to B360Blue).forEach { pair ->
+                val (label, value) = pair.first
+                val color = pair.second
+                Card(
+                    modifier = Modifier.weight(1f),
+                    shape = RoundedCornerShape(12.dp),
+                    colors = CardDefaults.cardColors(containerColor = Color.White),
+                    border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                ) {
+                    Column(Modifier.padding(20.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
+                        Text(label, fontSize = 13.sp, color = Color(0xFF64748B), fontWeight = FontWeight.Medium)
+                        Text(value, fontWeight = FontWeight.Bold, fontSize = 20.sp, color = color)
+                    }
+                }
+            }
+        }
+
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
+                Text(
+                    text = "Tax Summary",
+                    fontWeight = FontWeight.Bold,
+                    style = MaterialTheme.typography.titleMedium,
+                    color = Color(0xFF1E293B)
+                )
+                HorizontalDivider(color = Color(0xFFE2E8F0))
+                taxTypes.forEach { (type, amount, due) ->
+                    Row(
+                        Modifier.fillMaxWidth().padding(vertical = 4.dp),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(type, fontSize = 14.sp, color = Color(0xFF1E293B), fontWeight = FontWeight.Medium)
+                        Text(amount, fontSize = 14.sp, fontWeight = FontWeight.Bold, color = Color(0xFF1E293B))
+                        Text(due, fontSize = 12.sp, color = Color(0xFF64748B))
+                    }
+                    HorizontalDivider(color = Color(0xFFF1F5F9))
+                }
+            }
+        }
+    }
+}
+
+@Composable
+fun DesktopKraScreen() {
+    val returns = listOf(
+        Triple("VAT3 - Feb 2025", "Submitted", "KES 48,000"),
+        Triple("TOT - Feb 2025", "Pending", "KES 4,500"),
+        Triple("WHT - Feb 2025", "Pending", "KES 9,000"),
+    )
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "KRA iTax Integration",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Verify eTIMS logs and file pending returns",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
+            listOf("Compliance Score" to "87%" to B360Blue, "eTIMS Invoices" to "142" to B360Green, "Pending Returns" to "2" to B360Amber).forEach { pair ->
+                val (label, value) = pair.first
+                val color = pair.second
+                Card(
+                    modifier = Modifier.weight(1f),
+                    shape = RoundedCornerShape(12.dp),
+                    colors = CardDefaults.cardColors(containerColor = Color.White),
+                    border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                ) {
+                    Column(Modifier.padding(20.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
+                        Text(label, fontSize = 13.sp, color = Color(0xFF64748B), fontWeight = FontWeight.Medium)
+                        Text(value, fontWeight = FontWeight.Bold, fontSize = 20.sp, color = color)
+                    }
+                }
+            }
+        }
+
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
+                Row(
+                    Modifier.fillMaxWidth(),
+                    horizontalArrangement = Arrangement.SpaceBetween,
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    Text(
+                        text = "Tax Returns",
+                        fontWeight = FontWeight.Bold,
+                        style = MaterialTheme.typography.titleMedium,
+                        color = Color(0xFF1E293B)
+                    )
+                    OutlinedButton(
+                        onClick = {},
+                        shape = RoundedCornerShape(8.dp),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        colors = ButtonDefaults.outlinedButtonColors(contentColor = Color(0xFF475569))
+                    ) {
+                        Text("Download CSV", fontSize = 13.sp)
+                    }
+                }
+                HorizontalDivider(color = Color(0xFFE2E8F0))
+                returns.forEach { (name, status, amount) ->
+                    Row(
+                        Modifier.fillMaxWidth().padding(vertical = 4.dp),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(name, fontSize = 14.sp, color = Color(0xFF1E293B), fontWeight = FontWeight.Medium)
+                        Text(amount, fontSize = 14.sp, fontWeight = FontWeight.Bold, color = Color(0xFF1E293B))
+                        val statusColor = if (status == "Submitted") B360Green else B360Amber
+                        Surface(
+                            color = statusColor.copy(0.12f),
+                            shape = RoundedCornerShape(12.dp)
+                        ) {
+                            Text(
+                                text = status,
+                                fontSize = 11.sp,
+                                color = statusColor,
+                                fontWeight = FontWeight.SemiBold,
+                                modifier = Modifier.padding(horizontal = 8.dp, vertical = 3.dp)
+                            )
+                        }
+                    }
+                    HorizontalDivider(color = Color(0xFFF1F5F9))
+                }
+            }
+        }
+    }
+}
+
+// ── Social Inbox Screen ───────────────────────────────────────────────────────
+data class PlatformItem(
+    val id: String,
+    val name: String,
+    val desc: String,
+    val emoji: String,
+    val color: Color
+)
+
+@Composable
+fun DesktopSocialScreen(
+    viewModel: SocialViewModel = remember { inject() }
+) {
+    val state by viewModel.state.collectAsState()
+
+    LaunchedEffect(Unit) {
+        viewModel.loadChannelsAndInbox()
+    }
+
+    if (state.isLoading) {
+        Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
+            CircularProgressIndicator(color = B360Green)
+        }
+    } else if (state.channels.isEmpty()) {
+        SocialOnboardingWizard(viewModel, state)
+    } else {
+        UnifiedInboxView(viewModel, state)
+    }
+}
+
+@Composable
+fun SocialOnboardingWizard(viewModel: SocialViewModel, state: SocialState) {
+    Column(
+        modifier = Modifier
+            .fillMaxSize()
+            .background(Color(0xFFF8FAFC))
+            .padding(32.dp),
+        horizontalAlignment = Alignment.CenterHorizontally,
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        // Wizard Header
+        Text(
+            text = "Social Commerce Channel Setup",
+            style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+            color = Color(0xFF1E293B)
+        )
+        
+        Text(
+            text = "Link your social channels to enable automated replies and trigger direct payment requests.",
+            style = MaterialTheme.typography.bodyMedium,
+            color = Color(0xFF64748B)
+        )
+
+        // Step indicator banner
+        Row(
+            modifier = Modifier.fillMaxWidth().widthIn(max = 800.dp),
+            horizontalArrangement = Arrangement.spacedBy(8.dp)
+        ) {
+            val stepNames = listOf("1. Platform", "2. Credentials", "3. Webhooks", "4. Scan Verification", "5. AI Persona")
+            stepNames.forEachIndexed { idx, _ ->
+                val isActive = idx == state.onboardingStep
+                val isCompleted = idx < state.onboardingStep
+                val color = if (isActive) B360Green else if (isCompleted) Color(0xFF10B981) else Color(0xFFCBD5E1)
+                
+                Box(
+                    modifier = Modifier
+                        .weight(1f)
+                        .height(4.dp)
+                        .background(color, RoundedCornerShape(2.dp))
+                )
+            }
+        }
+
+        // Error message card
+        val errorMsg = state.error
+        if (errorMsg != null) {
+            Card(
+                modifier = Modifier.fillMaxWidth().widthIn(max = 800.dp),
+                colors = CardDefaults.cardColors(containerColor = Color(0xFFFEF2F2)),
+                border = BorderStroke(1.dp, Color(0xFFFCA5A5))
+            ) {
+                Row(
+                    modifier = Modifier.padding(16.dp),
+                    horizontalArrangement = Arrangement.SpaceBetween,
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    Text(errorMsg, color = Color(0xFF991B1B), style = MaterialTheme.typography.bodyMedium)
+                    IconButton(onClick = { viewModel.dismissError() }) {
+                        Icon(Icons.Default.Close, contentDescription = "Dismiss", tint = Color(0xFF991B1B))
+                    }
+                }
+            }
+        }
+
+        // Active Wizard Content
+        Box(
+            modifier = Modifier
+                .weight(1f)
+                .fillMaxWidth()
+                .widthIn(max = 800.dp)
+        ) {
+            when (state.onboardingStep) {
+                0 -> StepChoosePlatform(viewModel)
+                1 -> StepEnterCredentials(viewModel, state)
+                2 -> StepWebhookDetails(viewModel, state)
+                3 -> StepVerificationScan(viewModel, state)
+                4 -> StepConfigureAi(viewModel, state)
+            }
+        }
+    }
+}
+
+@Composable
+fun StepChoosePlatform(viewModel: SocialViewModel) {
+    val platforms = listOf(
+        PlatformItem("WHATSAPP", "WhatsApp Business", "Direct customer chat on WhatsApp, support STK prompts", "💬", Color(0xFF25D366)),
+        PlatformItem("INSTAGRAM", "Instagram Direct", "Receive direct messages and comments from Instagram shop", "📸", Color(0xFFE1306C)),
+        PlatformItem("FACEBOOK", "Messenger", "Connect your business page messenger chat directly", "📘", Color(0xFF1877F2)),
+        PlatformItem("TIKTOK", "TikTok Business", "Respond to DMs and viral comments with ease", "🎵", Color(0xFF000000))
+    )
+
+    Column(
+        verticalArrangement = Arrangement.spacedBy(16.dp),
+        horizontalAlignment = Alignment.CenterHorizontally
+    ) {
+        Text("Select a Platform", style = MaterialTheme.typography.titleLarge.copy(fontWeight = FontWeight.Bold))
+        Spacer(Modifier.height(8.dp))
+        Row(
+            modifier = Modifier.fillMaxWidth(),
+            horizontalArrangement = Arrangement.spacedBy(16.dp)
+        ) {
+            platforms.forEach { platform ->
+                Card(
+                    modifier = Modifier
+                        .weight(1f)
+                        .height(180.dp)
+                        .clickable { viewModel.selectPlatform(platform.id) },
+                    border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                    colors = CardDefaults.cardColors(containerColor = Color.White)
+                ) {
+                    Column(
+                        modifier = Modifier.fillMaxSize().padding(16.dp),
+                        verticalArrangement = Arrangement.SpaceBetween,
+                        horizontalAlignment = Alignment.Start
+                    ) {
+                        Box(
+                            modifier = Modifier
+                                .size(40.dp)
+                                .clip(CircleShape)
+                                .background(platform.color.copy(alpha = 0.1f)),
+                            contentAlignment = Alignment.Center
+                        ) {
+                            Text(platform.emoji, fontSize = 20.sp)
+                        }
+                        Column {
+                            Text(platform.name, fontWeight = FontWeight.Bold, fontSize = 15.sp, color = Color(0xFF1E293B))
+                            Spacer(Modifier.height(4.dp))
+                            Text(platform.desc, fontSize = 11.sp, color = Color(0xFF64748B))
+                        }
+                    }
+                }
+            }
+        }
+    }
+}
+
+@Composable
+fun StepEnterCredentials(viewModel: SocialViewModel, state: SocialState) {
+    val platformName = state.selectedPlatform?.lowercase()?.replaceFirstChar { it.uppercase() } ?: ""
+    Column(
+        modifier = Modifier.fillMaxSize().verticalScroll(rememberScrollState()),
+        verticalArrangement = Arrangement.spacedBy(16.dp)
+    ) {
+        Text("Enter $platformName Credentials", style = MaterialTheme.typography.titleLarge.copy(fontWeight = FontWeight.Bold))
+
+        Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+            Text("Channel Display Name", fontSize = 12.sp, color = Color.Gray, fontWeight = FontWeight.Medium)
+            OutlinedTextField(
+                value = state.channelNameInput,
+                onValueChange = { viewModel.updateNameInput(it) },
+                modifier = Modifier.fillMaxWidth(),
+                placeholder = { Text("e.g. My Shop $platformName") },
+                shape = RoundedCornerShape(8.dp)
+            )
+        }
+
+        if (state.selectedPlatform == "WHATSAPP") {
+            Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                Text("WhatsApp Business Phone Number ID", fontSize = 12.sp, color = Color.Gray, fontWeight = FontWeight.Medium)
+                OutlinedTextField(
+                    value = state.phoneNumberInput,
+                    onValueChange = { viewModel.updatePhoneNumberInput(it) },
+                    modifier = Modifier.fillMaxWidth(),
+                    placeholder = { Text("e.g. +254712345678") },
+                    shape = RoundedCornerShape(8.dp)
+                )
+            }
+        }
+
+        Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+            val idLabel = when (state.selectedPlatform) {
+                "WHATSAPP" -> "WhatsApp Business Account ID (WABA ID)"
+                "FACEBOOK" -> "Facebook Page ID"
+                "INSTAGRAM" -> "Instagram Professional Account ID"
+                else -> "TikTok Developer Client ID"
+            }
+            Text(idLabel, fontSize = 12.sp, color = Color.Gray, fontWeight = FontWeight.Medium)
+            OutlinedTextField(
+                value = state.externalIdInput,
+                onValueChange = { viewModel.updateExternalIdInput(it) },
+                modifier = Modifier.fillMaxWidth(),
+                placeholder = { Text("e.g. 109848729384729") },
+                shape = RoundedCornerShape(8.dp)
+            )
+        }
+
+        Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+            Text("Developer Access Token", fontSize = 12.sp, color = Color.Gray, fontWeight = FontWeight.Medium)
+            OutlinedTextField(
+                value = state.accessTokenInput,
+                onValueChange = { viewModel.updateAccessTokenInput(it) },
+                modifier = Modifier.fillMaxWidth().height(100.dp),
+                placeholder = { Text("Paste your system user or page access token here...") },
+                shape = RoundedCornerShape(8.dp)
+            )
+        }
+
+        Spacer(Modifier.height(16.dp))
+
+        Row(
+            modifier = Modifier.fillMaxWidth(),
+            horizontalArrangement = Arrangement.SpaceBetween
+        ) {
+            OutlinedButton(
+                onClick = { viewModel.resetWizard() },
+                shape = RoundedCornerShape(8.dp)
+            ) {
+                Text("Back")
+            }
+
+            Button(
+                onClick = { viewModel.saveCredentials() },
+                colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                shape = RoundedCornerShape(8.dp),
+                enabled = !state.savingChannel
+            ) {
+                if (state.savingChannel) {
+                    CircularProgressIndicator(modifier = Modifier.size(16.dp), color = Color.White, strokeWidth = 2.dp)
+                } else {
+                    Text("Register Channel")
+                }
+            }
+        }
+    }
+}
+
+@Composable
+fun StepWebhookDetails(viewModel: SocialViewModel, state: SocialState) {
+    val channel = state.createdChannel ?: return
+    val clipboardManager = androidx.compose.ui.platform.LocalClipboardManager.current
+    
+    Column(
+        modifier = Modifier.fillMaxSize().verticalScroll(rememberScrollState()),
+        verticalArrangement = Arrangement.spacedBy(20.dp)
+    ) {
+        Text("Configure Webhooks", style = MaterialTheme.typography.titleLarge.copy(fontWeight = FontWeight.Bold))
+        Text("Please register the following endpoint parameters in your developer console to enable real-time message receiving.", fontSize = 13.sp, color = Color.Gray)
+
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+            colors = CardDefaults.cardColors(containerColor = Color(0xFFF1F5F9))
+        ) {
+            Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
+                Column {
+                    Text("WEBHOOK CALLBACK URL", fontWeight = FontWeight.Bold, fontSize = 11.sp, color = Color(0xFF475569))
+                    Spacer(Modifier.height(4.dp))
+                    Row(
+                        modifier = Modifier.fillMaxWidth(),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(channel.webhookUrl, style = MaterialTheme.typography.bodyMedium.copy(fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace))
+                        Text(
+                            text = "Copy",
+                            color = B360Green,
+                            fontWeight = FontWeight.Bold,
+                            fontSize = 12.sp,
+                            modifier = Modifier.clickable { clipboardManager.setText(androidx.compose.ui.text.AnnotatedString(channel.webhookUrl)) }.padding(4.dp)
+                        )
+                    }
+                }
+                
+                HorizontalDivider(color = Color(0xFFCBD5E1))
+
+                Column {
+                    Text("VERIFY TOKEN", fontWeight = FontWeight.Bold, fontSize = 11.sp, color = Color(0xFF475569))
+                    Spacer(Modifier.height(4.dp))
+                    Row(
+                        modifier = Modifier.fillMaxWidth(),
+                        horizontalArrangement = Arrangement.SpaceBetween,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(channel.webhookVerifyToken, style = MaterialTheme.typography.bodyMedium.copy(fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace))
+                        Text(
+                            text = "Copy",
+                            color = B360Green,
+                            fontWeight = FontWeight.Bold,
+                            fontSize = 12.sp,
+                            modifier = Modifier.clickable { clipboardManager.setText(androidx.compose.ui.text.AnnotatedString(channel.webhookVerifyToken)) }.padding(4.dp)
+                        )
+                    }
+                }
+            }
+        }
+
+        Spacer(Modifier.height(16.dp))
+
+        Row(
+            modifier = Modifier.fillMaxWidth(),
+            horizontalArrangement = Arrangement.SpaceBetween
+        ) {
+            OutlinedButton(
+                onClick = { viewModel.prevStep() },
+                shape = RoundedCornerShape(8.dp)
+            ) {
+                Text("Back")
+            }
+
+            Button(
+                onClick = { viewModel.nextStep() },
+                colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                shape = RoundedCornerShape(8.dp)
+            ) {
+                Text("Webhook is Configured")
+            }
+        }
+    }
+}
+
+@Composable
+fun StepVerificationScan(viewModel: SocialViewModel, state: SocialState) {
+    Column(
+        modifier = Modifier.fillMaxSize(),
+        verticalArrangement = Arrangement.spacedBy(20.dp),
+        horizontalAlignment = Alignment.CenterHorizontally
+    ) {
+        Text("Test Connection Status", style = MaterialTheme.typography.titleLarge.copy(fontWeight = FontWeight.Bold))
+
+        if (state.verificationStage == "idle") {
+            Button(
+                onClick = { viewModel.runVerificationScan() },
+                colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                shape = RoundedCornerShape(8.dp)
+            ) {
+                Text("Start Handshake Verification Scan")
+            }
+        } else {
+            Row(
+                modifier = Modifier.fillMaxWidth(),
+                horizontalArrangement = Arrangement.SpaceBetween,
+                verticalAlignment = Alignment.CenterVertically
+            ) {
+                Text(
+                    text = if (state.verificationStage == "testing") "Scanning integration rails..." else "Integration Successful!",
+                    fontWeight = FontWeight.SemiBold,
+                    fontSize = 14.sp
+                )
+                Text("${(state.verifyProgress * 100).toInt()}%", fontSize = 14.sp)
+            }
+
+            LinearProgressIndicator(
+                progress = state.verifyProgress,
+                modifier = Modifier.fillMaxWidth().height(8.dp).clip(RoundedCornerShape(4.dp)),
+                color = B360Green,
+                trackColor = Color(0xFFCBD5E1)
+            )
+
+            Card(
+                modifier = Modifier.fillMaxWidth().weight(1f),
+                colors = CardDefaults.cardColors(containerColor = Color(0xFF0F172A)),
+                shape = RoundedCornerShape(10.dp)
+            ) {
+                LazyColumn(
+                    modifier = Modifier.fillMaxSize().padding(16.dp),
+                    verticalArrangement = Arrangement.spacedBy(8.dp)
+                ) {
+                    items(state.verificationLogs) { log ->
+                        Text(
+                            text = "> $log",
+                            color = Color(0xFF38BDF8),
+                            style = MaterialTheme.typography.bodySmall.copy(
+                                fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace
+                            )
+                        )
                     }
                 }
             }
+
+            if (state.verificationStage == "success") {
+                Button(
+                    onClick = { viewModel.nextStep() },
+                    colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                    shape = RoundedCornerShape(8.dp)
+                ) {
+                    Text("Proceed to AI Persona Setup")
+                }
+            }
         }
     }
 }
 
-// ─── Reports ──────────────────────────────────────────────────────────────────
-
 @Composable
-fun DesktopReportsScreen(
-    viewModel: ReportsViewModel = remember { inject() }
-) {
-    val state by viewModel.state.collectAsState()
-    LaunchedEffect(Unit) {
-        viewModel.loadReport("This Month")
-    }
-
-    val summary = state.profitSummary
+fun StepConfigureAi(viewModel: SocialViewModel, state: SocialState) {
+    Column(
+        modifier = Modifier.fillMaxSize().verticalScroll(rememberScrollState()),
+        verticalArrangement = Arrangement.spacedBy(20.dp)
+    ) {
+        Text("AI Sales Assistant Configuration", style = MaterialTheme.typography.titleLarge.copy(fontWeight = FontWeight.Bold))
 
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
-        Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
-            // P&L Summary
-            Card(Modifier.weight(1f), shape = RoundedCornerShape(12.dp)) {
-                Column(Modifier.padding(20.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) {
-                    Text("Profit & Loss — March 2025", fontWeight = FontWeight.Bold)
-                    HorizontalDivider()
-                    if (summary == null) {
-                        Text("Loading profit summary...", color = Color.Gray, modifier = Modifier.padding(vertical = 12.dp))
-                    } else {
-                        PnlRow("Total Revenue", "KES ${String.format("%,.0f", summary.totalRevenue)}", B360Green)
-                        PnlRow("Cost of Goods Sold", "KES ${String.format("%,.0f", summary.totalCostOfGoods)}", B360Red)
-                        PnlRow("Gross Profit", "KES ${String.format("%,.0f", summary.grossProfit)}", B360Green, bold = true)
-                        PnlRow("Total Expenses", "KES ${String.format("%,.0f", summary.totalExpenses)}", B360Red)
-                        HorizontalDivider()
-                        PnlRow("Net Profit", "KES ${String.format("%,.0f", summary.netProfit)}", B360Green, bold = true, large = true)
-                        PnlRow("Net Margin", "${String.format("%.1f", summary.netMargin * 100)}%", B360Blue)
+        Card(
+            modifier = Modifier.fillMaxWidth(),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+            colors = CardDefaults.cardColors(containerColor = Color.White)
+        ) {
+            Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
+                Row(
+                    modifier = Modifier.fillMaxWidth(),
+                    horizontalArrangement = Arrangement.SpaceBetween,
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    Column {
+                        Text("Enable AI Auto-Replies", fontWeight = FontWeight.Bold, fontSize = 14.sp)
+                        Text("Let the AI assistant answer catalog, inventory, and order questions automatically.", fontSize = 12.sp, color = Color.Gray)
                     }
+                    Switch(
+                        checked = state.autoReplyEnabledInput,
+                        onCheckedChange = { viewModel.updateAutoReplyInput(it) },
+                        colors = SwitchDefaults.colors(
+                            checkedThumbColor = Color.White,
+                            checkedTrackColor = B360Green
+                        )
+                    )
                 }
-            }
-            // Expense breakdown
-            Card(Modifier.weight(1f), shape = RoundedCornerShape(12.dp)) {
-                Column(Modifier.padding(20.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) {
-                    Text("Expense Breakdown", fontWeight = FontWeight.Bold)
+
+                if (state.autoReplyEnabledInput) {
                     HorizontalDivider()
-                    listOf(
-                        Triple("Stock Purchase", "KES 45,000", 0.69f),
-                        Triple("Advertising", "KES 8,500", 0.13f),
-                        Triple("Rent", "KES 15,000", 0.23f),
-                        Triple("Delivery", "KES 3,200", 0.05f),
-                        Triple("Packaging", "KES 1,200", 0.02f)
-                    ).forEach { (cat, amt, frac) ->
-                        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
-                            Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween) {
-                                Text(cat, fontSize = 13.sp)
-                                Text(amt, fontWeight = FontWeight.SemiBold, fontSize = 13.sp)
-                            }
-                            LinearProgressIndicator(progress = frac, Modifier.fillMaxWidth().height(6.dp), color = B360Green, trackColor = B360Green.copy(0.15f))
-                        }
+                    Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                        Text("AI Persona & Instruction Prompt", fontSize = 12.sp, color = Color.Gray, fontWeight = FontWeight.Medium)
+                        OutlinedTextField(
+                            value = state.aiPersonaPromptInput,
+                            onValueChange = { viewModel.updatePersonaPromptInput(it) },
+                            modifier = Modifier.fillMaxWidth().height(120.dp),
+                            shape = RoundedCornerShape(8.dp)
+                        )
+                        Text("Customize how the AI talks, what language it uses (e.g. Swahili, Sheng), and customer policies.", fontSize = 11.sp, color = Color.Gray)
                     }
                 }
             }
         }
-        // Export buttons
-        Row(horizontalArrangement = Arrangement.spacedBy(12.dp)) {
-            OutlinedButton(onClick = {}) { Icon(Icons.Filled.PictureAsPdf, null, Modifier.size(16.dp)); Spacer(Modifier.width(6.dp)); Text("Export PDF") }
-            OutlinedButton(onClick = {}) { Icon(Icons.Filled.TableChart, null, Modifier.size(16.dp)); Spacer(Modifier.width(6.dp)); Text("Export Excel") }
-            OutlinedButton(onClick = {}) { Icon(Icons.Filled.Share, null, Modifier.size(16.dp)); Spacer(Modifier.width(6.dp)); Text("Share via WhatsApp") }
+
+        Spacer(Modifier.height(16.dp))
+
+        Row(
+            modifier = Modifier.fillMaxWidth(),
+            horizontalArrangement = Arrangement.SpaceBetween
+        ) {
+            OutlinedButton(
+                onClick = { viewModel.prevStep() },
+                shape = RoundedCornerShape(8.dp)
+            ) {
+                Text("Back")
+            }
+
+            Button(
+                onClick = { viewModel.saveAiPersona() },
+                colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                shape = RoundedCornerShape(8.dp),
+                enabled = !state.savingChannel
+            ) {
+                if (state.savingChannel) {
+                    CircularProgressIndicator(modifier = Modifier.size(16.dp), color = Color.White, strokeWidth = 2.dp)
+                } else {
+                    Text("Complete & Go to Inbox")
+                }
+            }
         }
     }
 }
 
 @Composable
-fun PnlRow(label: String, value: String, color: Color, bold: Boolean = false, large: Boolean = false) {
-    Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween) {
-        Text(label, fontWeight = if (bold) FontWeight.Bold else FontWeight.Normal, fontSize = if (large) 15.sp else 13.sp)
-        Text(value, color = color, fontWeight = if (bold) FontWeight.Bold else FontWeight.SemiBold, fontSize = if (large) 15.sp else 13.sp)
+fun UnifiedInboxView(viewModel: SocialViewModel, state: SocialState) {
+    var searchKeyword by remember { mutableStateOf("") }
+    
+    var paymentAmount by remember { mutableStateOf("") }
+    var paymentDesc by remember { mutableStateOf("") }
+
+    val filteredConversations = state.conversations.filter {
+        it.customerName.contains(searchKeyword, ignoreCase = true) ||
+        (it.customerPhone ?: "").contains(searchKeyword)
     }
-}
 
-// ─── Settings ─────────────────────────────────────────────────────────────────
+    val activeConv = state.conversations.find { it.id == state.activeConversationId }
 
-@Composable
-fun DesktopSettingsScreen() {
-    val scrollState = rememberScrollState()
-    // Load current URL from the same source used at startup
-    var backendUrl by remember { mutableStateOf(com.app.biashara.data.remote.BASE_URL) }
-    Column(
-        Modifier.fillMaxSize().verticalScroll(scrollState).padding(24.dp),
-        verticalArrangement = Arrangement.spacedBy(20.dp)
-    ) {
-        SettingsSection("Business Profile") {
-            SettingsField("Business Name", "Wanjiru's Fashion")
-            SettingsField("Owner Phone", "+254712345678")
-            SettingsField("Business Type", "Retail")
-            SettingsField("Mpesa Short Code", "174379")
-        }
-        SettingsSection("Security") {
-            SettingsToggle("Two-Factor Authentication (2FA)", true)
-            SettingsToggle("Email Notifications", true)
-            SettingsToggle("SMS Alerts", false)
-        }
-        SettingsSection("Subscription") {
-            Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, verticalAlignment = Alignment.CenterVertically) {
-                Column { Text("Current Plan", fontWeight = FontWeight.Medium); Text("Freemium", color = Color.Gray) }
-                Button(onClick = {}, colors = ButtonDefaults.buttonColors(B360Green)) { Text("Upgrade to Premium") }
+    Row(modifier = Modifier.fillMaxSize().background(Color(0xFFF1F5F9))) {
+        // ── Left: Conversations List ──
+        Column(
+            modifier = Modifier
+                .width(320.dp)
+                .fillMaxHeight()
+                .background(Color.White)
+                .border(BorderStroke(1.dp, Color(0xFFE2E8F0)))
+        ) {
+            Row(
+                modifier = Modifier.fillMaxWidth().padding(16.dp),
+                horizontalArrangement = Arrangement.SpaceBetween,
+                verticalAlignment = Alignment.CenterVertically
+            ) {
+                Text("Unified Inbox", style = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold))
+                IconButton(onClick = { viewModel.loadInbox() }) {
+                    Icon(Icons.Default.Refresh, contentDescription = "Refresh Inbox", modifier = Modifier.size(20.dp))
+                }
             }
-        }
-        // New section for backend configuration
-        SettingsSection("Backend Connectivity") {
+
             OutlinedTextField(
-                value = backendUrl,
-                onValueChange = { backendUrl = it },
-                label = { Text("Backend URL") },
-                singleLine = true,
+                value = searchKeyword,
+                onValueChange = { searchKeyword = it },
+                placeholder = { Text("Search conversations...", fontSize = 13.sp) },
+                modifier = Modifier.fillMaxWidth().padding(horizontal = 16.dp).padding(bottom = 12.dp),
                 shape = RoundedCornerShape(8.dp),
-                modifier = Modifier.fillMaxWidth()
+                singleLine = true,
+                leadingIcon = { Icon(Icons.Default.Search, contentDescription = null, modifier = Modifier.size(18.dp)) }
             )
-            Spacer(Modifier.height(12.dp))
-            Button(
-                onClick = {
-                    // Save to config file
-                    val configDir = File(System.getProperty("user.home"), ".biashara360")
-                    if (!configDir.exists()) configDir.mkdirs()
-                    val configFile = File(configDir, "base_url.txt")
-                    configFile.writeText(backendUrl.trim())
-                    // Update runtime variable
-                    com.app.biashara.data.remote.BASE_URL = backendUrl.trim()
-                },
-                colors = ButtonDefaults.buttonColors(B360Green)
-            ) {
-                Text("Save Backend URL")
-            }
-        }
-    }
-}
 
-@Composable
-fun SettingsSection(title: String, content: @Composable ColumnScope.() -> Unit) {
-    Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(12.dp)) {
-        Column(Modifier.padding(20.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
-            Text(title, fontWeight = FontWeight.Bold, fontSize = 15.sp)
             HorizontalDivider()
-            content()
-        }
-    }
-}
 
-@Composable
-fun SettingsField(label: String, value: String) {
-    Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, Alignment.CenterVertically) {
-        Text(label, color = Color.Gray)
-        OutlinedTextField(value = value, onValueChange = {}, modifier = Modifier.width(300.dp), shape = RoundedCornerShape(8.dp), singleLine = true)
-    }
-}
+            LazyColumn(
+                modifier = Modifier.fillMaxSize().weight(1f)
+            ) {
+                items(filteredConversations) { conv ->
+                    val isSelected = conv.id == state.activeConversationId
+                    val platformColor = when (conv.platform.uppercase()) {
+                        "WHATSAPP" -> Color(0xFF25D366)
+                        "INSTAGRAM" -> Color(0xFFE1306C)
+                        "FACEBOOK" -> Color(0xFF1877F2)
+                        else -> Color(0xFF000000)
+                    }
 
-@Composable
-fun SettingsToggle(label: String, checked: Boolean) {
-    var state by remember { mutableStateOf(checked) }
-    Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, Alignment.CenterVertically) {
-        Text(label)
-        Switch(checked = state, onCheckedChange = { state = it }, colors = SwitchDefaults.colors(checkedThumbColor = Color.White, checkedTrackColor = B360Green))
-    }
-}
+                    Box(
+                        modifier = Modifier
+                            .fillMaxWidth()
+                            .clickable { viewModel.selectConversation(conv.id) }
+                            .background(if (isSelected) Color(0xFFF1F5F9) else Color.White)
+                            .padding(16.dp)
+                    ) {
+                        Row(
+                            horizontalArrangement = Arrangement.spacedBy(12.dp),
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            Box(
+                                modifier = Modifier
+                                    .size(40.dp)
+                                    .clip(CircleShape)
+                                    .background(platformColor.copy(alpha = 0.1f)),
+                                contentAlignment = Alignment.Center
+                            ) {
+                                Text(
+                                    text = conv.customerName.first().toString(),
+                                    fontWeight = FontWeight.Bold,
+                                    color = platformColor,
+                                    fontSize = 16.sp
+                                )
+                            }
 
-// ── Tax Screen ────────────────────────────────────────────────────────────────
-@Composable
-fun DesktopTaxScreen() {
-    val taxTypes = listOf(
-        Triple("VAT 16%", "KES 48,000", "Due 20th"),
-        Triple("TOT 1.5%", "KES 4,500", "Due 20th"),
-        Triple("WHT 3%", "KES 9,000", "Due 20th"),
-    )
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
-        Text("Tax Management", fontWeight = FontWeight.Bold, fontSize = 20.sp)
+                            Column(modifier = Modifier.weight(1f)) {
+                                Row(
+                                    modifier = Modifier.fillMaxWidth(),
+                                    horizontalArrangement = Arrangement.SpaceBetween,
+                                    verticalAlignment = Alignment.CenterVertically
+                                ) {
+                                    Text(
+                                        text = conv.customerName,
+                                        fontWeight = if (conv.unreadCount > 0) FontWeight.Bold else FontWeight.SemiBold,
+                                        fontSize = 13.sp,
+                                        color = Color(0xFF1E293B)
+                                    )
+                                    Text(
+                                        text = conv.platform.lowercase().replaceFirstChar { it.uppercase() },
+                                        fontSize = 10.sp,
+                                        fontWeight = FontWeight.Bold,
+                                        color = platformColor
+                                    )
+                                }
+                                Spacer(Modifier.height(4.dp))
+                                Text(
+                                    text = conv.lastMessage,
+                                    fontSize = 11.sp,
+                                    color = Color(0xFF64748B),
+                                    maxLines = 1
+                                )
+                            }
 
-        Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
-            listOf("VAT Liability" to "KES 48,000", "TOT Liability" to "KES 4,500", "Next Filing" to "Mar 20").forEach { (label, value) ->
-                Card(Modifier.weight(1f), shape = RoundedCornerShape(12.dp)) {
-                    Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
-                        Text(label, fontSize = 12.sp, color = Color.Gray)
-                        Text(value, fontWeight = FontWeight.Bold, fontSize = 18.sp, color = B360Green)
+                            if (conv.unreadCount > 0) {
+                                Box(
+                                    modifier = Modifier
+                                        .size(18.dp)
+                                        .clip(CircleShape)
+                                        .background(B360Green),
+                                    contentAlignment = Alignment.Center
+                                ) {
+                                    Text(
+                                        text = conv.unreadCount.toString(),
+                                        color = Color.White,
+                                        fontSize = 9.sp,
+                                        fontWeight = FontWeight.Bold
+                                    )
+                                }
+                            }
+                        }
                     }
+                    HorizontalDivider(color = Color(0xFFF1F5F9))
                 }
             }
         }
 
-        Card(shape = RoundedCornerShape(12.dp)) {
-            Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) {
-                Text("Tax Summary", fontWeight = FontWeight.SemiBold, fontSize = 16.sp)
-                taxTypes.forEach { (type, amount, due) ->
-                    Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween) {
-                        Text(type, fontSize = 14.sp)
-                        Text(amount, fontSize = 14.sp, fontWeight = FontWeight.Medium)
-                        Text(due, fontSize = 12.sp, color = Color.Gray)
+        // ── Center: Chat Panel ──
+        Column(
+            modifier = Modifier
+                .weight(1f)
+                .fillMaxHeight()
+                .background(Color(0xFFF8FAFC))
+                .border(BorderStroke(1.dp, Color(0xFFE2E8F0)))
+        ) {
+            if (activeConv == null) {
+                Box(modifier = Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
+                    Column(horizontalAlignment = Alignment.CenterHorizontally) {
+                        Icon(Icons.Default.Forum, contentDescription = null, tint = Color.LightGray, modifier = Modifier.size(64.dp))
+                        Spacer(Modifier.height(16.dp))
+                        Text("Select a conversation to start chatting", color = Color.Gray, fontSize = 14.sp)
                     }
-                    HorizontalDivider(color = Color(0xFFF0F0F0))
                 }
-            }
-        }
-    }
-}
+            } else {
+                Row(
+                    modifier = Modifier.fillMaxWidth().background(Color.White).padding(16.dp),
+                    horizontalArrangement = Arrangement.SpaceBetween,
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    Row(
+                        horizontalArrangement = Arrangement.spacedBy(12.dp),
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text(activeConv.customerName, fontWeight = FontWeight.Bold, fontSize = 16.sp)
+                        if (activeConv.isAiHandled) {
+                            Box(
+                                modifier = Modifier
+                                    .clip(RoundedCornerShape(12.dp))
+                                    .background(B360Green.copy(0.15f))
+                                    .padding(horizontal = 8.dp, vertical = 2.dp)
+                            ) {
+                                Text("AI Handled", color = B360Green, fontSize = 10.sp, fontWeight = FontWeight.Bold)
+                            }
+                        }
+                    }
 
-// ── KRA iTax Screen ───────────────────────────────────────────────────────────
-@Composable
-fun DesktopKraScreen() {
-    val returns = listOf(
-        Triple("VAT3 - Feb 2025", "Submitted", "KES 48,000"),
-        Triple("TOT - Feb 2025", "Pending", "KES 4,500"),
-        Triple("WHT - Feb 2025", "Pending", "KES 9,000"),
-    )
-    Column(Modifier.fillMaxSize().padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
-        Text("KRA iTax Integration", fontWeight = FontWeight.Bold, fontSize = 20.sp)
+                    Text(
+                        text = "Last active: ${activeConv.lastMessageAt}",
+                        fontSize = 11.sp,
+                        color = Color.Gray
+                    )
+                }
 
-        Row(horizontalArrangement = Arrangement.spacedBy(16.dp)) {
-            listOf("Compliance Score" to "87%", "eTIMS Invoices" to "142", "Pending Returns" to "2").forEach { (label, value) ->
-                Card(Modifier.weight(1f), shape = RoundedCornerShape(12.dp)) {
-                    Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
-                        Text(label, fontSize = 12.sp, color = Color.Gray)
-                        Text(value, fontWeight = FontWeight.Bold, fontSize = 18.sp, color = B360Green)
+                HorizontalDivider()
+
+                LazyColumn(
+                    modifier = Modifier.fillMaxWidth().weight(1f).padding(16.dp),
+                    verticalArrangement = Arrangement.spacedBy(12.dp),
+                    reverseLayout = false
+                ) {
+                    items(state.messages) { msg ->
+                        val isOutbound = msg.direction == "OUTBOUND"
+                        val senderTag = when (msg.senderType) {
+                            "AI" -> "AI Assistant"
+                            "AGENT" -> "Agent"
+                            else -> null
+                        }
+
+                        Row(
+                            modifier = Modifier.fillMaxWidth(),
+                            horizontalArrangement = if (isOutbound) Arrangement.End else Arrangement.Start
+                        ) {
+                            Card(
+                                shape = RoundedCornerShape(
+                                    topStart = 12.dp,
+                                    topEnd = 12.dp,
+                                    bottomStart = if (isOutbound) 12.dp else 0.dp,
+                                    bottomEnd = if (isOutbound) 0.dp else 12.dp
+                                ),
+                                colors = CardDefaults.cardColors(
+                                    containerColor = if (isOutbound) B360Green else Color.White
+                                ),
+                                border = if (isOutbound) null else BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                                modifier = Modifier.widthIn(max = 400.dp)
+                            ) {
+                                Column(modifier = Modifier.padding(12.dp)) {
+                                    if (senderTag != null) {
+                                        Text(
+                                            text = senderTag,
+                                            fontSize = 9.sp,
+                                            fontWeight = FontWeight.Bold,
+                                            color = if (isOutbound) Color.White.copy(alpha = 0.8f) else B360Green,
+                                            modifier = Modifier.padding(bottom = 2.dp)
+                                        )
+                                    }
+                                    Text(
+                                        text = msg.content,
+                                        color = if (isOutbound) Color.White else Color(0xFF1E293B),
+                                        fontSize = 13.sp
+                                    )
+                                }
+                            }
+                        }
                     }
                 }
-            }
-        }
 
-        Card(shape = RoundedCornerShape(12.dp)) {
-            Column(Modifier.padding(16.dp), verticalArrangement = Arrangement.spacedBy(12.dp)) {
-                Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, verticalAlignment = Alignment.CenterVertically) {
-                    Text("Tax Returns", fontWeight = FontWeight.SemiBold, fontSize = 16.sp)
-                    Button(onClick = {}, colors = ButtonDefaults.buttonColors(containerColor = B360Green)) {
-                        Text("Download CSV", fontSize = 13.sp)
+                if (state.suggestedReplies.isNotEmpty()) {
+                    Row(
+                        modifier = Modifier
+                            .fillMaxWidth()
+                            .background(Color(0xFFF1F5F9))
+                            .padding(12.dp),
+                        horizontalArrangement = Arrangement.spacedBy(8.dp),
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Text("✨ AI Suggested Reply:", fontSize = 11.sp, fontWeight = FontWeight.Bold, color = Color(0xFF92400E))
+                        state.suggestedReplies.forEach { reply ->
+                            Card(
+                                modifier = Modifier
+                                    .weight(1f)
+                                    .clickable { viewModel.handleSendMessage(reply) },
+                                colors = CardDefaults.cardColors(containerColor = Color.White),
+                                border = BorderStroke(1.dp, Color(0xFFFCD34D)),
+                                shape = RoundedCornerShape(8.dp)
+                            ) {
+                                Text(
+                                    text = reply,
+                                    fontSize = 12.sp,
+                                    maxLines = 2,
+                                    modifier = Modifier.padding(8.dp),
+                                    color = Color(0xFF1E293B)
+                                )
+                            }
+                        }
                     }
                 }
-                returns.forEach { (name, status, amount) ->
-                    Row(Modifier.fillMaxWidth(), horizontalArrangement = Arrangement.SpaceBetween, verticalAlignment = Alignment.CenterVertically) {
-                        Text(name, fontSize = 14.sp)
-                        Text(amount, fontSize = 14.sp, fontWeight = FontWeight.Medium)
-                        val statusColor = if (status == "Submitted") B360Green else Color(0xFFFF8F00)
-                        Text(status, fontSize = 12.sp, color = statusColor, fontWeight = FontWeight.SemiBold)
+
+                HorizontalDivider()
+
+                var typedMessage by remember { mutableStateOf("") }
+                Row(
+                    modifier = Modifier.fillMaxWidth().background(Color.White).padding(12.dp),
+                    horizontalArrangement = Arrangement.spacedBy(8.dp),
+                    verticalAlignment = Alignment.CenterVertically
+                ) {
+                    Button(
+                        onClick = { viewModel.generateAiSuggestion() },
+                        colors = ButtonDefaults.buttonColors(containerColor = Color(0xFFFEF3C7)),
+                        shape = RoundedCornerShape(20.dp),
+                        border = BorderStroke(1.dp, Color(0xFFFCD34D)),
+                        enabled = !state.aiLoading,
+                        modifier = Modifier.height(36.dp)
+                    ) {
+                        Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(4.dp)) {
+                            if (state.aiLoading) {
+                                CircularProgressIndicator(modifier = Modifier.size(14.dp), color = Color(0xFFD97706), strokeWidth = 1.5.dp)
+                            } else {
+                                Text("✨ AI Reply", color = Color(0xFFD97706), fontWeight = FontWeight.Bold, fontSize = 12.sp)
+                            }
+                        }
+                    }
+
+                    OutlinedTextField(
+                        value = typedMessage,
+                        onValueChange = { typedMessage = it },
+                        placeholder = { Text("Type a message...", fontSize = 13.sp) },
+                        modifier = Modifier.weight(1f),
+                        shape = RoundedCornerShape(20.dp),
+                        singleLine = true
+                    )
+
+                    Button(
+                        onClick = {
+                            if (typedMessage.isNotBlank()) {
+                                viewModel.handleSendMessage(typedMessage)
+                                typedMessage = ""
+                            }
+                        },
+                        colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                        shape = RoundedCornerShape(20.dp),
+                        enabled = typedMessage.isNotBlank() && !state.isSendingMessage,
+                        modifier = Modifier.height(36.dp)
+                    ) {
+                        Text("Send", fontWeight = FontWeight.Bold)
                     }
-                    HorizontalDivider(color = Color(0xFFF0F0F0))
                 }
             }
         }
-    }
-}
 
-// ── Social Inbox Screen ───────────────────────────────────────────────────────
-@Composable
-fun DesktopSocialScreen() {
-    val conversations = listOf(
-        Triple("Amara Osei", "WhatsApp", "Do you have Nike size 42?"),
-        Triple("Fatuma Amin", "Instagram", "What's the price of the dress?"),
-        Triple("James Kariuki", "Facebook", "Can I pay via Mpesa?"),
-        Triple("Grace Mwangi", "TikTok", "Hi, I want to order 2 pieces"),
-    )
-    val platformColor = mapOf("WhatsApp" to Color(0xFF25D366), "Instagram" to Color(0xFFE1306C), "Facebook" to Color(0xFF1877F2), "TikTok" to Color(0xFF000000))
+        // ── Right: Tooling / Customer Profile Panel ──
+        if (activeConv != null) {
+            Column(
+                modifier = Modifier
+                    .width(280.dp)
+                    .fillMaxHeight()
+                    .background(Color.White)
+                    .padding(16.dp),
+                verticalArrangement = Arrangement.spacedBy(20.dp)
+            ) {
+                Text("Customer Details", style = MaterialTheme.typography.titleMedium.copy(fontWeight = FontWeight.Bold))
 
-    Row(Modifier.fillMaxSize()) {
-        // Conversation list
-        Column(Modifier.width(320.dp).fillMaxHeight().background(Color.White).padding(16.dp), verticalArrangement = Arrangement.spacedBy(8.dp)) {
-            Text("Unified Inbox", fontWeight = FontWeight.Bold, fontSize = 16.sp)
-            HorizontalDivider()
-            conversations.forEach { (name, platform, msg) ->
-                Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(8.dp)) {
-                    Row(Modifier.padding(12.dp), verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(10.dp)) {
-                        Box(Modifier.size(36.dp).clip(CircleShape).background(platformColor[platform] ?: B360Green), contentAlignment = Alignment.Center) {
-                            Text(name.first().toString(), color = Color.White, fontWeight = FontWeight.Bold)
+                Card(
+                    modifier = Modifier.fillMaxWidth(),
+                    shape = RoundedCornerShape(10.dp),
+                    border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                    colors = CardDefaults.cardColors(containerColor = Color(0xFFF8FAFC))
+                ) {
+                    Column(modifier = Modifier.padding(12.dp), verticalArrangement = Arrangement.spacedBy(10.dp)) {
+                        Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(8.dp)) {
+                            Icon(Icons.Default.Person, contentDescription = null, tint = Color.Gray, modifier = Modifier.size(16.dp))
+                            Text(activeConv.customerName, fontWeight = FontWeight.SemiBold, fontSize = 13.sp)
                         }
-                        Column(Modifier.weight(1f)) {
-                            Text(name, fontSize = 13.sp, fontWeight = FontWeight.SemiBold)
-                            Text(msg, fontSize = 11.sp, color = Color.Gray, maxLines = 1)
+                        val phone = activeConv.customerPhone
+                        if (phone != null) {
+                            Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.spacedBy(8.dp)) {
+                                Icon(Icons.Default.Phone, contentDescription = null, tint = Color.Gray, modifier = Modifier.size(16.dp))
+                                Text(phone, fontSize = 12.sp, color = Color.Gray)
+                            }
                         }
-                        Text(platform, fontSize = 10.sp, color = platformColor[platform] ?: B360Green)
+                    }
+                }
+
+                HorizontalDivider()
+
+                Text("Send Payment Prompt", style = MaterialTheme.typography.titleSmall.copy(fontWeight = FontWeight.Bold))
+
+                Column(verticalArrangement = Arrangement.spacedBy(12.dp)) {
+                    Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+                        Text("Amount (KES)", fontSize = 11.sp, color = Color.Gray)
+                        OutlinedTextField(
+                            value = paymentAmount,
+                            onValueChange = { paymentAmount = it },
+                            placeholder = { Text("e.g. 2500") },
+                            modifier = Modifier.fillMaxWidth(),
+                            singleLine = true,
+                            shape = RoundedCornerShape(8.dp)
+                        )
+                    }
+
+                    Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+                        Text("Description", fontSize = 11.sp, color = Color.Gray)
+                        OutlinedTextField(
+                            value = paymentDesc,
+                            onValueChange = { paymentDesc = it },
+                            placeholder = { Text("e.g. Invoice #12 — Shoes") },
+                            modifier = Modifier.fillMaxWidth(),
+                            shape = RoundedCornerShape(8.dp)
+                        )
+                    }
+
+                    Button(
+                        onClick = {
+                            val amt = paymentAmount.toDoubleOrNull()
+                            if (amt != null && amt > 0 && paymentDesc.isNotBlank()) {
+                                viewModel.handleSendPaymentPrompt(amt, paymentDesc)
+                                paymentAmount = ""
+                                paymentDesc = ""
+                            }
+                        },
+                        colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(8.dp),
+                        enabled = paymentAmount.isNotBlank() && paymentDesc.isNotBlank() && !state.isSendingMessage
+                    ) {
+                        Text("💸 Send Link Prompt", fontSize = 13.sp, fontWeight = FontWeight.Bold)
                     }
                 }
             }
         }
-        VerticalDivider(Modifier.fillMaxHeight().width(1.dp))
-        // Chat panel placeholder
-        Column(Modifier.fillMaxSize().padding(32.dp), horizontalAlignment = Alignment.CenterHorizontally, verticalArrangement = Arrangement.Center) {
-            Icon(Icons.Filled.Forum, null, tint = Color.LightGray, modifier = Modifier.size(64.dp))
-            Spacer(Modifier.height(16.dp))
-            Text("Select a conversation", color = Color.Gray, fontSize = 16.sp)
-        }
     }
 }
 
@@ -1067,52 +4012,92 @@ fun DesktopCyberSourceSettingsScreen() {
         Modifier
             .fillMaxSize()
             .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
             .padding(24.dp),
-        verticalArrangement = Arrangement.spacedBy(20.dp)
+        verticalArrangement = Arrangement.spacedBy(24.dp)
     ) {
-        Text("CyberSource Configuration", fontWeight = FontWeight.Bold, fontSize = 22.sp)
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "CyberSource Configuration",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Manage your CyberSource card payment gateway credentials",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
 
-        Card(Modifier.fillMaxWidth(), shape = RoundedCornerShape(12.dp)) {
-            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(16.dp)) {
-                Text("API Credentials", fontWeight = FontWeight.Bold, fontSize = 16.sp)
-                HorizontalDivider()
+        Card(
+            Modifier.fillMaxWidth(),
+            shape = RoundedCornerShape(12.dp),
+            colors = CardDefaults.cardColors(containerColor = Color.White),
+            border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+        ) {
+            Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+                Text(
+                    text = "API Credentials",
+                    fontWeight = FontWeight.Bold,
+                    style = MaterialTheme.typography.titleMedium,
+                    color = Color(0xFF1E293B)
+                )
+                HorizontalDivider(color = Color(0xFFE2E8F0))
 
                 // Merchant ID
                 Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
-                    Text("Merchant ID (Organization ID)", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color.Gray)
+                    Text("Merchant ID (Organization ID)", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
                     OutlinedTextField(
                         value = merchantId,
                         onValueChange = { merchantId = it },
                         modifier = Modifier.fillMaxWidth(),
                         shape = RoundedCornerShape(8.dp),
                         singleLine = true,
-                        placeholder = { Text("e.g. wanfashion_cs_098") }
+                        placeholder = { Text("e.g. wanfashion_cs_098") },
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
                     )
                 }
 
                 // Key ID
                 Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
-                    Text("Active Key ID (REST API JWT/P12 Key ID)", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color.Gray)
+                    Text("Active Key ID (REST API JWT/P12 Key ID)", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
                     OutlinedTextField(
                         value = merchantKeyId,
                         onValueChange = { merchantKeyId = it },
                         modifier = Modifier.fillMaxWidth(),
                         shape = RoundedCornerShape(8.dp),
                         singleLine = true,
-                        placeholder = { Text("e.g. 9c7c25eb-xxxx-xxxx-xxxx-xxxxxxx") }
+                        placeholder = { Text("e.g. 9c7c25eb-xxxx-xxxx-xxxx-xxxxxxx") },
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
                     )
                 }
 
                 // Secret Key
                 Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
-                    Text("Shared Secret Key (Rest API Shared Secret)", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color.Gray)
+                    Text("Shared Secret Key (Rest API Shared Secret)", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
                     OutlinedTextField(
                         value = merchantSecretKey,
                         onValueChange = { merchantSecretKey = it },
                         modifier = Modifier.fillMaxWidth(),
                         shape = RoundedCornerShape(8.dp),
                         singleLine = true,
-                        placeholder = { Text("Enter your secure merchant shared secret key") }
+                        placeholder = { Text("Enter your secure merchant shared secret key") },
+                        colors = OutlinedTextFieldDefaults.colors(
+                            focusedBorderColor = B360Green,
+                            unfocusedBorderColor = Color(0xFFE2E8F0),
+                            focusedContainerColor = Color.White,
+                            unfocusedContainerColor = Color.White
+                        )
                     )
                 }
 
@@ -1123,8 +4108,9 @@ fun DesktopCyberSourceSettingsScreen() {
                     verticalAlignment = Alignment.CenterVertically
                 ) {
                     Column {
-                        Text("Active Sandbox Environment", fontWeight = FontWeight.Medium)
-                        Text("Toggle off to deploy credentials on live CyberSource production rails", color = Color.Gray, fontSize = 12.sp)
+                        Text("Active Sandbox Environment", fontWeight = FontWeight.Bold, color = Color(0xFF1E293B), fontSize = 14.sp)
+                        Spacer(Modifier.height(2.dp))
+                        Text("Toggle off to deploy credentials on live CyberSource production rails", color = Color(0xFF64748B), fontSize = 12.sp)
                     }
                     Switch(
                         checked = isSandbox,
@@ -1136,14 +4122,14 @@ fun DesktopCyberSourceSettingsScreen() {
                     )
                 }
 
-                HorizontalDivider()
+                HorizontalDivider(color = Color(0xFFE2E8F0))
 
                 if (showSuccessMessage) {
                     Box(
                         modifier = Modifier
                             .fillMaxWidth()
                             .clip(RoundedCornerShape(8.dp))
-                            .background(B360Green.copy(0.15f))
+                            .background(B360Green.copy(0.12f))
                             .padding(14.dp)
                     ) {
                         Text("✓ CyberSource configuration validated and saved successfully!", color = B360Green, fontWeight = FontWeight.SemiBold, fontSize = 13.sp)
@@ -1163,7 +4149,854 @@ fun DesktopCyberSourceSettingsScreen() {
                         shape = RoundedCornerShape(8.dp),
                         modifier = Modifier.width(180.dp)
                     ) {
-                        Text("Save Configuration", fontWeight = FontWeight.Bold)
+                        Text("Save Configuration", fontWeight = FontWeight.Bold, color = Color.White)
+                    }
+                }
+            }
+        }
+    }
+}
+
+@Composable
+fun DesktopMpesaScreen(
+    viewModel: BusinessViewModel = remember { inject() }
+) {
+    val state by viewModel.mpesaState.collectAsState()
+    
+    LaunchedEffect(Unit) {
+        viewModel.loadMpesaConfig()
+    }
+
+    var consumerKey by remember { mutableStateOf("") }
+    var consumerSecret by remember { mutableStateOf("") }
+    var passKey by remember { mutableStateOf("") }
+    var shortCode by remember { mutableStateOf("") }
+    var callbackUrl by remember { mutableStateOf("") }
+    var environment by remember { mutableStateOf("sandbox") }
+    var accountType by remember { mutableStateOf("paybill") }
+
+    LaunchedEffect(state.config) {
+        state.config?.let { cfg ->
+            consumerKey = cfg.consumerKey
+            consumerSecret = "••••••••••••••••••••••••••••••••"
+            passKey = "••••••••••••••••••••••••••••••••"
+            shortCode = cfg.shortCode
+            callbackUrl = cfg.callbackUrl
+            environment = cfg.environment
+            accountType = cfg.accountType
+        }
+    }
+
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        // Header
+        Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+            Text(
+                text = "M-Pesa Integration Settings",
+                style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                color = Color(0xFF1E293B)
+            )
+            Text(
+                text = "Configure your Safaricom Daraja API keys for real-time mobile checkout",
+                style = MaterialTheme.typography.bodyMedium,
+                color = Color(0xFF64748B)
+            )
+        }
+
+        if (state.isLoading) {
+            Box(Modifier.fillMaxWidth().height(200.dp), contentAlignment = Alignment.Center) {
+                CircularProgressIndicator(color = B360Green)
+            }
+        } else {
+            // Error Card
+            state.error?.let { err ->
+                Card(
+                    colors = CardDefaults.cardColors(containerColor = Color(0xFFFEF2F2)),
+                    border = BorderStroke(1.dp, Color(0xFFFCA5A5)),
+                    modifier = Modifier.fillMaxWidth()
+                ) {
+                    Text(
+                        text = err,
+                        color = Color(0xFFB91C1C),
+                        fontSize = 13.sp,
+                        fontWeight = FontWeight.SemiBold,
+                        modifier = Modifier.padding(16.dp)
+                    )
+                }
+            }
+
+            // Success Card
+            if (state.saveSuccess) {
+                Card(
+                    colors = CardDefaults.cardColors(containerColor = Color(0xFFECFDF5)),
+                    border = BorderStroke(1.dp, Color(0xFFA7F3D0)),
+                    modifier = Modifier.fillMaxWidth()
+                ) {
+                    Text(
+                        text = "✓ M-Pesa configuration saved successfully!",
+                        color = Color(0xFF065F46),
+                        fontSize = 13.sp,
+                        fontWeight = FontWeight.SemiBold,
+                        modifier = Modifier.padding(16.dp)
+                    )
+                }
+            }
+
+            Card(
+                Modifier.fillMaxWidth(),
+                shape = RoundedCornerShape(12.dp),
+                colors = CardDefaults.cardColors(containerColor = Color.White),
+                border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+            ) {
+                Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+                    Text(
+                        text = "Daraja API Configurations",
+                        fontWeight = FontWeight.Bold,
+                        style = MaterialTheme.typography.titleMedium,
+                        color = Color(0xFF1E293B)
+                    )
+                    HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                    // Consumer Key
+                    Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                        Text("Consumer Key *", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                        OutlinedTextField(
+                            value = consumerKey,
+                            onValueChange = { consumerKey = it },
+                            modifier = Modifier.fillMaxWidth(),
+                            shape = RoundedCornerShape(8.dp),
+                            singleLine = true,
+                            placeholder = { Text("Enter Consumer Key") },
+                            colors = OutlinedTextFieldDefaults.colors(
+                                focusedBorderColor = B360Green,
+                                unfocusedBorderColor = Color(0xFFE2E8F0)
+                            )
+                        )
+                    }
+
+                    // Consumer Secret
+                    Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                        Text("Consumer Secret *", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                        OutlinedTextField(
+                            value = consumerSecret,
+                            onValueChange = { consumerSecret = it },
+                            modifier = Modifier.fillMaxWidth(),
+                            shape = RoundedCornerShape(8.dp),
+                            singleLine = true,
+                            visualTransformation = PasswordVisualTransformation(),
+                            placeholder = { Text("Enter Consumer Secret") },
+                            colors = OutlinedTextFieldDefaults.colors(
+                                focusedBorderColor = B360Green,
+                                unfocusedBorderColor = Color(0xFFE2E8F0)
+                            )
+                        )
+                    }
+
+                    // Passkey
+                    Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                        Text("Passkey *", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                        OutlinedTextField(
+                            value = passKey,
+                            onValueChange = { passKey = it },
+                            modifier = Modifier.fillMaxWidth(),
+                            shape = RoundedCornerShape(8.dp),
+                            singleLine = true,
+                            visualTransformation = PasswordVisualTransformation(),
+                            placeholder = { Text("Enter LNM Passkey") },
+                            colors = OutlinedTextFieldDefaults.colors(
+                                focusedBorderColor = B360Green,
+                                unfocusedBorderColor = Color(0xFFE2E8F0)
+                            )
+                        )
+                    }
+
+                    // Shortcode
+                    Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                        Text("Business Shortcode (Paybill / Till) *", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                        OutlinedTextField(
+                            value = shortCode,
+                            onValueChange = { shortCode = it },
+                            modifier = Modifier.fillMaxWidth(),
+                            shape = RoundedCornerShape(8.dp),
+                            singleLine = true,
+                            placeholder = { Text("e.g. 174379") },
+                            colors = OutlinedTextFieldDefaults.colors(
+                                focusedBorderColor = B360Green,
+                                unfocusedBorderColor = Color(0xFFE2E8F0)
+                            )
+                        )
+                    }
+
+                    // Callback URL
+                    Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                        Text("Callback URL *", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                        OutlinedTextField(
+                            value = callbackUrl,
+                            onValueChange = { callbackUrl = it },
+                            modifier = Modifier.fillMaxWidth(),
+                            shape = RoundedCornerShape(8.dp),
+                            singleLine = true,
+                            placeholder = { Text("https://api.yourdomain.com/v1/payments/mpesa/callback") },
+                            colors = OutlinedTextFieldDefaults.colors(
+                                focusedBorderColor = B360Green,
+                                unfocusedBorderColor = Color(0xFFE2E8F0)
+                            )
+                        )
+                    }
+
+                    // Select Dropdowns
+                    Row(
+                        modifier = Modifier.fillMaxWidth(),
+                        horizontalArrangement = Arrangement.spacedBy(16.dp)
+                    ) {
+                        // Environment Dropdown
+                        Box(modifier = Modifier.weight(1f)) {
+                            var envExpanded by remember { mutableStateOf(false) }
+                            Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                                Text("Environment", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                                Box {
+                                    OutlinedTextField(
+                                        value = if (environment == "sandbox") "Sandbox" else "Production",
+                                        onValueChange = {},
+                                        readOnly = true,
+                                        modifier = Modifier.fillMaxWidth(),
+                                        trailingIcon = {
+                                            IconButton(onClick = { envExpanded = true }) {
+                                                Icon(Icons.Filled.ArrowDropDown, null)
+                                            }
+                                        },
+                                        colors = OutlinedTextFieldDefaults.colors(
+                                            focusedBorderColor = B360Green,
+                                            unfocusedBorderColor = Color(0xFFE2E8F0)
+                                        )
+                                    )
+                                    DropdownMenu(expanded = envExpanded, onDismissRequest = { envExpanded = false }) {
+                                        DropdownMenuItem(
+                                            text = { Text("Sandbox") },
+                                            onClick = { environment = "sandbox"; envExpanded = false }
+                                        )
+                                        DropdownMenuItem(
+                                            text = { Text("Production") },
+                                            onClick = { environment = "production"; envExpanded = false }
+                                        )
+                                    }
+                                }
+                            }
+                        }
+
+                        // Account Type Dropdown
+                        Box(modifier = Modifier.weight(1f)) {
+                            var typeExpanded by remember { mutableStateOf(false) }
+                            Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                                Text("Account Type", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                                Box {
+                                    OutlinedTextField(
+                                        value = if (accountType == "paybill") "Paybill (C2B / LNM)" else "Buy Goods Till",
+                                        onValueChange = {},
+                                        readOnly = true,
+                                        modifier = Modifier.fillMaxWidth(),
+                                        trailingIcon = {
+                                            IconButton(onClick = { typeExpanded = true }) {
+                                                Icon(Icons.Filled.ArrowDropDown, null)
+                                            }
+                                        },
+                                        colors = OutlinedTextFieldDefaults.colors(
+                                            focusedBorderColor = B360Green,
+                                            unfocusedBorderColor = Color(0xFFE2E8F0)
+                                        )
+                                    )
+                                    DropdownMenu(expanded = typeExpanded, onDismissRequest = { typeExpanded = false }) {
+                                        DropdownMenuItem(
+                                            text = { Text("Paybill (C2B / LNM)") },
+                                            onClick = { accountType = "paybill"; typeExpanded = false }
+                                        )
+                                        DropdownMenuItem(
+                                            text = { Text("Buy Goods Till") },
+                                            onClick = { accountType = "till"; typeExpanded = false }
+                                        )
+                                    }
+                                }
+                            }
+                        }
+                    }
+
+                    // Save Button
+                    Row(
+                        modifier = Modifier.fillMaxWidth(),
+                        horizontalArrangement = Arrangement.End,
+                        verticalAlignment = Alignment.CenterVertically
+                    ) {
+                        Button(
+                            onClick = {
+                                viewModel.saveMpesaConfig(
+                                    MpesaConfigRequest(
+                                        consumerKey = consumerKey,
+                                        consumerSecret = consumerSecret,
+                                        passKey = passKey,
+                                        shortCode = shortCode,
+                                        callbackUrl = callbackUrl,
+                                        environment = environment,
+                                        accountType = accountType
+                                    )
+                                )
+                            },
+                            enabled = !state.isSaving,
+                            colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                            shape = RoundedCornerShape(8.dp),
+                            modifier = Modifier.width(180.dp)
+                        ) {
+                            if (state.isSaving) {
+                                CircularProgressIndicator(color = Color.White, modifier = Modifier.size(20.dp))
+                            } else {
+                                Text("Save Config", fontWeight = FontWeight.Bold, color = Color.White)
+                            }
+                        }
+                    }
+                }
+            }
+        }
+    }
+}
+
+@Composable
+fun DesktopReceiptTemplateScreen(
+    viewModel: BusinessViewModel = remember { inject() }
+) {
+    val state by viewModel.profileState.collectAsState()
+
+    LaunchedEffect(Unit) {
+        viewModel.loadProfile()
+    }
+
+    var header by remember { mutableStateOf("Welcome to our store!") }
+    var footer by remember { mutableStateOf("Thank you for shopping with us!") }
+    var showTax by remember { mutableStateOf(true) }
+    var showCustomer by remember { mutableStateOf(true) }
+
+    LaunchedEffect(state.profile) {
+        state.profile?.let { prof ->
+            header = prof.receiptHeader
+            footer = prof.receiptFooter
+            showTax = prof.receiptShowTax
+            showCustomer = prof.receiptShowCustomer
+        }
+    }
+
+    val scrollState = rememberScrollState()
+
+    Column(
+        Modifier
+            .fillMaxSize()
+            .verticalScroll(scrollState)
+            .background(Color(0xFFF8FAFC))
+            .padding(24.dp),
+        verticalArrangement = Arrangement.spacedBy(24.dp)
+    ) {
+        // Header Row
+        Row(
+            modifier = Modifier.fillMaxWidth(),
+            horizontalArrangement = Arrangement.SpaceBetween,
+            verticalAlignment = Alignment.CenterVertically
+        ) {
+            Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
+                Text(
+                    text = "Receipt Template Customization",
+                    style = MaterialTheme.typography.headlineMedium.copy(fontWeight = FontWeight.Bold),
+                    color = Color(0xFF1E293B)
+                )
+                Text(
+                    text = "Personalize the layout and details of your thermal receipts",
+                    style = MaterialTheme.typography.bodyMedium,
+                    color = Color(0xFF64748B)
+                )
+            }
+
+            Button(
+                onClick = {
+                    state.profile?.let { prof ->
+                        viewModel.updateProfile(
+                            prof.copy(
+                                receiptHeader = header,
+                                receiptFooter = footer,
+                                receiptShowTax = showTax,
+                                receiptShowCustomer = showCustomer
+                            )
+                        )
+                    }
+                },
+                enabled = !state.isSaving && state.profile != null,
+                colors = ButtonDefaults.buttonColors(containerColor = B360Green),
+                shape = RoundedCornerShape(8.dp),
+                modifier = Modifier.width(180.dp)
+            ) {
+                if (state.isSaving) {
+                    CircularProgressIndicator(color = Color.White, modifier = Modifier.size(20.dp))
+                } else {
+                    Text("Save Template", fontWeight = FontWeight.Bold, color = Color.White)
+                }
+            }
+        }
+
+        if (state.isLoading) {
+            Box(Modifier.fillMaxWidth().height(200.dp), contentAlignment = Alignment.Center) {
+                CircularProgressIndicator(color = B360Green)
+            }
+        } else {
+            // Error Card
+            state.error?.let { err ->
+                Card(
+                    colors = CardDefaults.cardColors(containerColor = Color(0xFFFEF2F2)),
+                    border = BorderStroke(1.dp, Color(0xFFFCA5A5)),
+                    modifier = Modifier.fillMaxWidth()
+                ) {
+                    Text(
+                        text = err,
+                        color = Color(0xFFB91C1C),
+                        fontSize = 13.sp,
+                        fontWeight = FontWeight.SemiBold,
+                        modifier = Modifier.padding(16.dp)
+                    )
+                }
+            }
+
+            // Success Card
+            if (state.saveSuccess) {
+                Card(
+                    colors = CardDefaults.cardColors(containerColor = Color(0xFFECFDF5)),
+                    border = BorderStroke(1.dp, Color(0xFFA7F3D0)),
+                    modifier = Modifier.fillMaxWidth()
+                ) {
+                    Text(
+                        text = "✓ Receipt template customized successfully!",
+                        color = Color(0xFF065F46),
+                        fontSize = 13.sp,
+                        fontWeight = FontWeight.SemiBold,
+                        modifier = Modifier.padding(16.dp)
+                    )
+                }
+            }
+
+            Row(
+                modifier = Modifier.fillMaxWidth(),
+                horizontalArrangement = Arrangement.spacedBy(24.dp),
+                verticalAlignment = Alignment.Top
+            ) {
+                // Left Side: Editor Form
+                Card(
+                    modifier = Modifier.weight(1.2f),
+                    shape = RoundedCornerShape(12.dp),
+                    colors = CardDefaults.cardColors(containerColor = Color.White),
+                    border = BorderStroke(1.dp, Color(0xFFE2E8F0))
+                ) {
+                    Column(Modifier.padding(24.dp), verticalArrangement = Arrangement.spacedBy(20.dp)) {
+                        Text(
+                            text = "Receipt Parameters",
+                            fontWeight = FontWeight.Bold,
+                            style = MaterialTheme.typography.titleMedium,
+                            color = Color(0xFF1E293B)
+                        )
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                        // Header Message
+                        Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                            Text("Header Message", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                            OutlinedTextField(
+                                value = header,
+                                onValueChange = { header = it },
+                                modifier = Modifier.fillMaxWidth(),
+                                shape = RoundedCornerShape(8.dp),
+                                singleLine = true,
+                                placeholder = { Text("e.g. Welcome to our store!") },
+                                colors = OutlinedTextFieldDefaults.colors(
+                                    focusedBorderColor = B360Green,
+                                    unfocusedBorderColor = Color(0xFFE2E8F0)
+                                )
+                            )
+                        }
+
+                        // Footer Note
+                        Column(verticalArrangement = Arrangement.spacedBy(6.dp)) {
+                            Text("Footer Note", fontWeight = FontWeight.Medium, fontSize = 13.sp, color = Color(0xFF64748B))
+                            OutlinedTextField(
+                                value = footer,
+                                onValueChange = { footer = it },
+                                modifier = Modifier.fillMaxWidth(),
+                                shape = RoundedCornerShape(8.dp),
+                                singleLine = true,
+                                placeholder = { Text("e.g. Thank you for shopping with us!") },
+                                colors = OutlinedTextFieldDefaults.colors(
+                                    focusedBorderColor = B360Green,
+                                    unfocusedBorderColor = Color(0xFFE2E8F0)
+                                )
+                            )
+                        }
+
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                        // Show Tax Toggle
+                        Row(
+                            modifier = Modifier.fillMaxWidth(),
+                            horizontalArrangement = Arrangement.SpaceBetween,
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            Column(modifier = Modifier.weight(1f)) {
+                                Text("Show KRA Tax Breakdown", fontWeight = FontWeight.SemiBold, fontSize = 14.sp, color = Color(0xFF1E293B))
+                                Text("Include VAT (16%) details on thermal receipts", fontSize = 12.sp, color = Color(0xFF64748B))
+                            }
+                            Switch(
+                                checked = showTax,
+                                onCheckedChange = { showTax = it },
+                                colors = SwitchDefaults.colors(
+                                    checkedThumbColor = Color.White,
+                                    checkedTrackColor = B360Green,
+                                    uncheckedThumbColor = Color(0xFF64748B),
+                                    uncheckedTrackColor = Color(0xFFE2E8F0)
+                                )
+                            )
+                        }
+
+                        HorizontalDivider(color = Color(0xFFE2E8F0))
+
+                        // Show Customer Toggle
+                        Row(
+                            modifier = Modifier.fillMaxWidth(),
+                            horizontalArrangement = Arrangement.SpaceBetween,
+                            verticalAlignment = Alignment.CenterVertically
+                        ) {
+                            Column(modifier = Modifier.weight(1f)) {
+                                Text("Show Customer Details", fontWeight = FontWeight.SemiBold, fontSize = 14.sp, color = Color(0xFF1E293B))
+                                Text("Include buyer name and phone on receipt header", fontSize = 12.sp, color = Color(0xFF64748B))
+                            }
+                            Switch(
+                                checked = showCustomer,
+                                onCheckedChange = { showCustomer = it },
+                                colors = SwitchDefaults.colors(
+                                    checkedThumbColor = Color.White,
+                                    checkedTrackColor = B360Green,
+                                    uncheckedThumbColor = Color(0xFF64748B),
+                                    uncheckedTrackColor = Color(0xFFE2E8F0)
+                                )
+                            )
+                        }
+                    }
+                }
+
+                // Right Side: Live Thermal Receipt Preview
+                Column(
+                    modifier = Modifier.weight(1f),
+                    verticalArrangement = Arrangement.spacedBy(12.dp)
+                ) {
+                    Text(
+                        text = "LIVE RECEIPT PREVIEW",
+                        fontWeight = FontWeight.Bold,
+                        fontSize = 12.sp,
+                        color = Color(0xFF64748B),
+                        letterSpacing = 0.5.sp
+                    )
+
+                    val profile = state.profile
+                    Card(
+                        modifier = Modifier.fillMaxWidth(),
+                        shape = RoundedCornerShape(4.dp),
+                        colors = CardDefaults.cardColors(containerColor = Color(0xFFFFFFF0)),
+                        border = BorderStroke(1.dp, Color(0xFFE2E8F0)),
+                        elevation = CardDefaults.cardElevation(defaultElevation = 2.dp)
+                    ) {
+                        Column(
+                            modifier = Modifier.padding(24.dp),
+                            horizontalAlignment = Alignment.CenterHorizontally,
+                            verticalArrangement = Arrangement.spacedBy(8.dp)
+                        ) {
+                            // Header business details
+                            Text(
+                                text = profile?.name?.uppercase() ?: "BIASHARA STORE",
+                                fontWeight = FontWeight.Bold,
+                                fontSize = 14.sp,
+                                fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                color = Color.Black
+                            )
+                            Column(
+                                horizontalAlignment = Alignment.CenterHorizontally,
+                                verticalArrangement = Arrangement.spacedBy(2.dp)
+                            ) {
+                                Text(
+                                    text = profile?.address ?: "123 Tom Mboya St",
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.DarkGray
+                                )
+                                Text(
+                                    text = "${profile?.county ?: "Nairobi"}, Kenya",
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.DarkGray
+                                )
+                                Text(
+                                    text = "Tel: ${profile?.phone ?: "+254 700 000 000"}",
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.DarkGray
+                                )
+                                if (!profile?.kraPin.isNullOrBlank()) {
+                                    Text(
+                                        text = "PIN: ${profile?.kraPin}",
+                                        fontSize = 11.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.DarkGray
+                                    )
+                                }
+                            }
+
+                            // Dashed Divider
+                            Text(
+                                text = "------------------------------------------",
+                                fontSize = 11.sp,
+                                fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                color = Color.Gray
+                            )
+
+                            // Customer Section if enabled
+                            if (showCustomer) {
+                                Column(
+                                    modifier = Modifier.fillMaxWidth(),
+                                    verticalArrangement = Arrangement.spacedBy(2.dp)
+                                ) {
+                                    Text(
+                                        text = "CUSTOMER: John Doe",
+                                        fontSize = 10.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.DarkGray
+                                    )
+                                    Text(
+                                        text = "PHONE: +254 711 222 333",
+                                        fontSize = 10.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.DarkGray
+                                    )
+                                }
+                                Text(
+                                    text = "------------------------------------------",
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.Gray
+                                )
+                            }
+
+                            // Items Header
+                            Row(
+                                modifier = Modifier.fillMaxWidth(),
+                                horizontalArrangement = Arrangement.SpaceBetween
+                            ) {
+                                Text(
+                                    text = "ITEM",
+                                    fontWeight = FontWeight.Bold,
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.Black
+                                )
+                                Text(
+                                    text = "QTY * PRICE = TOTAL",
+                                    fontWeight = FontWeight.Bold,
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.Black
+                                )
+                            }
+
+                            Text(
+                                text = "------------------------------------------",
+                                fontSize = 11.sp,
+                                fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                color = Color.Gray
+                            )
+
+                            // Sample Items
+                            Column(
+                                modifier = Modifier.fillMaxWidth(),
+                                verticalArrangement = Arrangement.spacedBy(4.dp)
+                            ) {
+                                Row(
+                                    modifier = Modifier.fillMaxWidth(),
+                                    horizontalArrangement = Arrangement.SpaceBetween
+                                ) {
+                                    Text(
+                                        text = "Men's Slim Fit Jeans",
+                                        fontSize = 11.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                    Text(
+                                        text = "1 * 2,500.00 = 2,500.00",
+                                        fontSize = 11.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                }
+                                Row(
+                                    modifier = Modifier.fillMaxWidth(),
+                                    horizontalArrangement = Arrangement.SpaceBetween
+                                ) {
+                                    Text(
+                                        text = "Casual Cotton Shirt",
+                                        fontSize = 11.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                    Text(
+                                        text = "2 * 1,200.00 = 2,400.00",
+                                        fontSize = 11.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                }
+                            }
+
+                            Text(
+                                text = "------------------------------------------",
+                                fontSize = 11.sp,
+                                fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                color = Color.Gray
+                            )
+
+                            // Calculation totals
+                            Column(
+                                modifier = Modifier.fillMaxWidth(),
+                                verticalArrangement = Arrangement.spacedBy(4.dp)
+                            ) {
+                                Row(
+                                    modifier = Modifier.fillMaxWidth(),
+                                    horizontalArrangement = Arrangement.SpaceBetween
+                                ) {
+                                    Text(
+                                        text = "SUBTOTAL:",
+                                        fontSize = 11.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                    Text(
+                                        text = "KES 4,900.00",
+                                        fontSize = 11.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                }
+
+                                if (showTax) {
+                                    Row(
+                                        modifier = Modifier.fillMaxWidth(),
+                                        horizontalArrangement = Arrangement.SpaceBetween
+                                    ) {
+                                        Text(
+                                            text = "VAT (16% INCL):",
+                                            fontSize = 10.sp,
+                                            fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                            color = Color.DarkGray
+                                        )
+                                        Text(
+                                            text = "KES 675.86",
+                                            fontSize = 10.sp,
+                                            fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                            color = Color.DarkGray
+                                        )
+                                    }
+                                    Row(
+                                        modifier = Modifier.fillMaxWidth(),
+                                        horizontalArrangement = Arrangement.SpaceBetween
+                                    ) {
+                                        Text(
+                                            text = "NET EXCL TAX:",
+                                            fontSize = 10.sp,
+                                            fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                            color = Color.DarkGray
+                                        )
+                                        Text(
+                                            text = "KES 4,224.14",
+                                            fontSize = 10.sp,
+                                            fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                            color = Color.DarkGray
+                                        )
+                                    }
+                                }
+
+                                Row(
+                                    modifier = Modifier.fillMaxWidth(),
+                                    horizontalArrangement = Arrangement.SpaceBetween
+                                ) {
+                                    Text(
+                                        text = "TOTAL:",
+                                        fontWeight = FontWeight.Bold,
+                                        fontSize = 12.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                    Text(
+                                        text = "KES 4,900.00",
+                                        fontWeight = FontWeight.Bold,
+                                        fontSize = 12.sp,
+                                        fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                        color = Color.Black
+                                    )
+                                }
+                            }
+
+                            Text(
+                                text = "------------------------------------------",
+                                fontSize = 11.sp,
+                                fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                color = Color.Gray
+                            )
+
+                            // Custom message header and footer
+                            Column(
+                                horizontalAlignment = Alignment.CenterHorizontally,
+                                verticalArrangement = Arrangement.spacedBy(4.dp)
+                            ) {
+                                Text(
+                                    text = header,
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    fontWeight = FontWeight.Bold,
+                                    color = Color.Black,
+                                    textAlign = androidx.compose.ui.text.style.TextAlign.Center
+                                )
+                                Text(
+                                    text = footer,
+                                    fontSize = 11.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    fontWeight = FontWeight.Bold,
+                                    color = Color.Black,
+                                    textAlign = androidx.compose.ui.text.style.TextAlign.Center
+                                )
+                            }
+
+                            Spacer(Modifier.height(4.dp))
+
+                            Column(
+                                horizontalAlignment = Alignment.CenterHorizontally,
+                                verticalArrangement = Arrangement.spacedBy(2.dp)
+                            ) {
+                                Text(
+                                    text = "eTIMS Invoice: #INV-2026-0091",
+                                    fontSize = 9.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.DarkGray
+                                )
+                                Text(
+                                    text = "Powered by Biashara360",
+                                    fontSize = 9.sp,
+                                    fontFamily = androidx.compose.ui.text.font.FontFamily.Monospace,
+                                    color = Color.Gray
+                                )
+                            }
+                        }
                     }
                 }
             }
@@ -1171,3 +5004,5 @@ fun DesktopCyberSourceSettingsScreen() {
     }
 }
 
+
+
